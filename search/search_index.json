{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"grelmicro","text":"<p>grelmicro is a lightweight toolkit for building Python applications that need to coordinate work across processes, workers, or services.</p> <p> </p> <p>Documentation: https://grelinfo.github.io/grelmicro/</p> <p>Source Code: https://github.com/grelinfo/grelmicro</p>"},{"location":"#vision","title":"Vision","text":"<p>grelmicro is for any Python application that needs to coordinate work across multiple processes: microservices, modular monoliths, multi-worker deployments, or traditional applications scaling beyond a single process.</p> <ul> <li> <p>Easy to use. Simple decorators and context managers that do the right thing out of the box. No boilerplate, no complex configuration.</p> </li> <li> <p>Async by default. All I/O operations use <code>async</code>/<code>await</code>. Async is the natural fit for applications that spend most of their time waiting on network and disk, and it integrates cleanly with FastAPI and FastStream.</p> </li> <li> <p>Backend-agnostic. Every feature is defined by a protocol, not tied to a specific technology. Swap Redis for PostgreSQL, or use the in-memory backend for tests, without changing application code.</p> </li> <li> <p>Lightweight. A toolkit, not a framework. Pick the modules you need, ignore the rest. Minimal configuration: just register a backend and start using it.</p> </li> </ul> <p>The long-term goal is to grow grelmicro into an enterprise-grade toolkit, and eventually rewrite performance-critical components in Rust for better throughput and safety.</p>"},{"location":"#overview","title":"Overview","text":""},{"location":"#cache","title":"Cache","text":"<p>The <code>cache</code> module provides a <code>@cached</code> decorator with per-key stampede protection. Choose the cache that fits your use case: <code>TTLCache</code> for fast in-memory caching within a single process, or <code>RedisCache</code> for shared caching across multiple processes.</p>"},{"location":"#synchronization-primitives","title":"Synchronization Primitives","text":"<p>The <code>sync</code> module provides distributed coordination primitives, technology-agnostic across Redis, PostgreSQL, SQLite, Kubernetes, and in-memory backends.</p> <ul> <li>Lock: Distributed lock for synchronizing access to shared resources.</li> <li>Task Lock: Distributed lock for scheduled tasks with minimum and maximum hold times.</li> <li>Leader Election: Single-leader election for running tasks only once in a cluster.</li> </ul>"},{"location":"#task-scheduler","title":"Task Scheduler","text":"<p>The <code>task</code> module provides periodic task execution with optional distributed locking for at-most-once semantics across workers. Not a replacement for Celery or APScheduler: it is lightweight, easy to use, and built for coordination.</p>"},{"location":"#resilience","title":"Resilience","text":"<p>The <code>resilience</code> module provides patterns to protect services from failures and overload.</p> <ul> <li>Circuit Breaker: Automatically detects repeated failures and temporarily blocks calls to unstable services.</li> <li>Rate Limiter: Limits the number of requests per time window using the GCRA algorithm with Redis or in-memory backends.</li> </ul>"},{"location":"#logging","title":"Logging","text":"<p>The <code>logging</code> module provides 12-factor-compliant logging with JSON/text formatting, configurable timezone, and environment variable configuration (<code>LOG_LEVEL</code>, <code>LOG_FORMAT</code>, <code>LOG_TIMEZONE</code>).</p>"},{"location":"#tracing","title":"Tracing","text":"<p>The <code>tracing</code> module provides unified instrumentation inspired by Rust's <code>tracing</code> crate. The <code>@instrument</code> decorator creates OTel spans and enriches log records with structured context automatically.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install grelmicro\n</code></pre>"},{"location":"#example","title":"Example","text":""},{"location":"#fastapi-integration","title":"FastAPI Integration","text":"<p>Create a file <code>main.py</code> with:</p> <pre><code>import json\nimport logging\nfrom contextlib import asynccontextmanager\n\nfrom fastapi import FastAPI, HTTPException, Request\n\nfrom grelmicro.cache import TTLCache, cached\nfrom grelmicro.cache.redis import RedisCacheBackend\nfrom grelmicro.logging import configure_logging\nfrom grelmicro.resilience.circuitbreaker import CircuitBreaker\nfrom grelmicro.resilience.errors import RateLimitExceededError\nfrom grelmicro.resilience.ratelimiter import RateLimiter\nfrom grelmicro.resilience.redis import RedisRateLimiterBackend\nfrom grelmicro.sync import LeaderElection, Lock\nfrom grelmicro.sync.redis import RedisSyncBackend\nfrom grelmicro.task import TaskManager\n\nlogger = logging.getLogger(__name__)\n\n# === grelmicro ===\ntask = TaskManager()\nsync_backend = RedisSyncBackend(\"redis://localhost:6379/0\")\ncache_backend = RedisCacheBackend(\"redis://localhost:6379/0\", prefix=\"myapp:\")\nrate_limit_backend = RedisRateLimiterBackend(\"redis://localhost:6379/0\")\nleader_election = LeaderElection(\"leader-election\")\ntask.add_task(leader_election)\n\ncache = TTLCache(\n    ttl=300,\n    serializer=lambda v: json.dumps(v).encode(),\n    deserializer=json.loads,\n)\n\n\n# === FastAPI ===\n@asynccontextmanager\nasync def lifespan(app):\n    configure_logging()\n    async with sync_backend, cache_backend, rate_limit_backend, task:\n        yield\n\n\napp = FastAPI(lifespan=lifespan)\n\n\n# --- Cache: avoid redundant database queries ---\n@cached(cache)\nasync def get_user(user_id: int) -&gt; dict:\n    return {\"id\": user_id, \"name\": \"Alice\"}\n\n\n@app.get(\"/users/{user_id}\")\nasync def read_user(user_id: int):\n    return await get_user(user_id)\n\n\n# --- Circuit Breaker: protect calls to an unreliable service ---\ncb = CircuitBreaker(\"my-service\")\n\n\n@app.get(\"/\")\nasync def read_root():\n    async with cb:\n        return {\"Hello\": \"World\"}\n\n\n# --- Rate Limiter: protect endpoints from overload ---\napi_limiter = RateLimiter(\"api\", limit=100, window=60)\n\n\n@app.get(\"/api\")\nasync def api_endpoint(request: Request):\n    try:\n        await api_limiter.acquire_or_raise(key=request.client.host)\n    except RateLimitExceededError as exc:\n        raise HTTPException(\n            status_code=429,\n            detail=\"Too many requests\",\n            headers={\"Retry-After\": str(int(exc.retry_after))},\n        )\n    return {\"status\": \"ok\"}\n\n\n# --- Distributed Lock: synchronize access to a shared resource ---\nlock = Lock(\"shared-resource\")\n\n\n@app.get(\"/protected\")\nasync def protected():\n    async with lock:\n        return {\"status\": \"ok\"}\n\n\n# --- Interval Task: run locally on every worker ---\n@task.interval(seconds=5)\ndef heartbeat():\n    logger.info(\"heartbeat\")\n\n\n# --- Distributed Task: run once per interval across all workers ---\n@task.interval(seconds=60, max_lock_seconds=300)\ndef cleanup():\n    logger.info(\"cleanup\")\n\n\n# --- Leader-gated Task: only the leader executes ---\n@task.interval(seconds=10, leader=leader_election)\ndef leader_only_task():\n    logger.info(\"leader task\")\n</code></pre>"},{"location":"#dependencies","title":"Dependencies","text":"<p>grelmicro depends on Pydantic v2+, AnyIO v4+, and FastDepends.</p>"},{"location":"#standard-dependencies","title":"<code>standard</code> Dependencies","text":"<p>When you install grelmicro with <code>pip install grelmicro[standard]</code> it comes with:</p> <ul> <li><code>orjson</code>: A fast, correct JSON library for Python.</li> </ul>"},{"location":"#redis-dependencies","title":"<code>redis</code> Dependencies","text":"<p>When you install grelmicro with <code>pip install grelmicro[redis]</code> it comes with:</p> <ul> <li><code>redis-py</code>: The Python interface to the Redis key-value store (the async interface depends on <code>asyncio</code>).</li> </ul>"},{"location":"#postgres-dependencies","title":"<code>postgres</code> Dependencies","text":"<p>When you install grelmicro with <code>pip install grelmicro[postgres]</code> it comes with:</p> <ul> <li><code>asyncpg</code>: The Python <code>asyncio</code> interface for PostgreSQL.</li> </ul>"},{"location":"#license","title":"License","text":"<p>This project is licensed under the terms of the MIT license.</p>"},{"location":"cache/","title":"Cache","text":"<p>The <code>cache</code> module provides caching with swappable backends and a <code>@cached</code> decorator for caching function results.</p> <p>The cache is technology-agnostic, supporting multiple backends (see more in the Backend section).</p> <ul> <li>TTLCache: Cache with per-entry TTL, optional maxsize with LRU eviction, and serialization.</li> <li>@cached: Decorator that caches function results automatically with stampede protection.</li> </ul>"},{"location":"cache/#backend","title":"Backend","text":"<p>You must load a cache backend before using <code>TTLCache</code>.</p> <p>Note</p> <p>Although grelmicro uses AnyIO for concurrency, the backends generally depend on <code>asyncio</code>, therefore Trio is not supported.</p> MemoryRedis <pre><code>from grelmicro.cache.memory import MemoryCacheBackend\n\nbackend = MemoryCacheBackend()\n</code></pre> <pre><code>from grelmicro.cache.redis import RedisCacheBackend\n\nbackend = RedisCacheBackend(\"redis://localhost:6379/0\", prefix=\"myapp:\")\n</code></pre> <p>Backends must be used as async context managers:</p> <pre><code>async with backend:\n    # cache operations here\n    ...\n</code></pre>"},{"location":"cache/#ttlcache","title":"TTLCache","text":"<p><code>TTLCache</code> is the main cache class. It delegates storage to the registered backend and handles TTL, optional maxsize with LRU eviction, serialization, and statistics.</p> <pre><code>from grelmicro.cache import TTLCache\n\n# Uses the registered backend (MemoryCacheBackend or RedisCacheBackend)\ncache = TTLCache(maxsize=100, ttl=300)\n\n# Or pass a backend explicitly\ncache = TTLCache(maxsize=100, ttl=300, backend=my_backend)\n</code></pre> <p>All <code>TTLCache</code> methods are async:</p> <pre><code>await cache.set(\"key\", b\"value\")\nresult = await cache.get(\"key\")\nawait cache.delete(\"key\")\nawait cache.clear()\n</code></pre>"},{"location":"cache/#serialization","title":"Serialization","text":"<p>Backends store raw bytes. To cache Python objects, provide a <code>serializer</code> and <code>deserializer</code>:</p> <pre><code>import json\n\ncache = TTLCache(\n    ttl=300,\n    serializer=lambda v: json.dumps(v).encode(),\n    deserializer=json.loads,\n)\n\nawait cache.set(\"user\", {\"id\": 1, \"name\": \"Alice\"})  # stored as JSON bytes\nuser = await cache.get(\"user\")  # returns dict\n</code></pre> <p>Without a serializer, only <code>bytes</code> values are accepted.</p>"},{"location":"cache/#per-entry-ttl","title":"Per-Entry TTL","text":"<p>Override the default TTL for individual entries:</p> <pre><code>await cache.set(\"session\", b\"token\", ttl=3600)  # 1 hour instead of default\n</code></pre>"},{"location":"cache/#cached-decorator","title":"@cached Decorator","text":"<p>The <code>@cached</code> decorator automatically caches function results. It works with both sync and async functions.</p> <pre><code>from grelmicro.cache import TTLCache, cached\n\ncache = TTLCache(ttl=300, serializer=..., deserializer=...)\n\n@cached(cache)\nasync def get_user(user_id: int) -&gt; dict:\n    return await db.fetch_user(user_id)\n</code></pre>"},{"location":"cache/#stampede-protection","title":"Stampede Protection","text":"<p>Set <code>lock=True</code> to prevent multiple concurrent callers from recomputing the same cache entry on a cache miss. When enabled, only one caller executes the function while all others block and wait until the result is available:</p> <pre><code>@cached(cache, lock=True)\nasync def get_user(user_id: int) -&gt; dict:\n    return await db.fetch_user(user_id)\n</code></pre> <p>Locking is per-key: concurrent misses on different keys proceed in parallel. Only callers hitting the same key are serialized, so one slow computation does not block unrelated keys.</p> <p>When to use: your cached function is expensive (database query, API call, heavy computation) and may be called concurrently with the same arguments.</p>"},{"location":"cache/#decorator-parameters","title":"Decorator Parameters","text":"Parameter Type Default Description <code>cache</code> <code>TTLCache</code> required The cache instance to store results in. <code>key_maker</code> <code>Callable</code> <code>None</code> Custom key generation function. Receives <code>(func, args, kwargs)</code>. <code>skip</code> <code>Callable</code> <code>None</code> Predicate receiving the result. Returns <code>True</code> to skip caching. <code>typed</code> <code>bool</code> <code>False</code> Cache arguments of different types separately. <code>lock</code> <code>bool</code> or context manager <code>None</code> Stampede protection. <code>True</code> enables per-key locking."},{"location":"cache/#redis-backend-configuration","title":"Redis Backend Configuration","text":"<p>The Redis URL can be passed directly or read from environment variables:</p> Environment Variable Description Default <code>REDIS_URL</code> Full Redis URL (e.g. <code>redis://localhost:6379/0</code>) <code>REDIS_HOST</code> Redis hostname <code>REDIS_PORT</code> Redis port <code>6379</code> <code>REDIS_DB</code> Redis database number <code>0</code> <code>REDIS_PASSWORD</code> Redis password <p>Set either <code>REDIS_URL</code> or <code>REDIS_HOST</code> (not both).</p> <p>Use the <code>prefix</code> parameter to isolate cache keys from other data in the same Redis instance.</p> <p>Warning</p> <p>Cache Key Stability: Cache keys are derived from <code>repr()</code> of function arguments. Keys are stable within a single process but may vary across Python versions. Objects with default <code>__repr__</code> (e.g., custom class instances) include memory addresses, which means cache misses will always occur. Use a custom <code>key_maker</code> for such objects.</p>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#0110-2026-04-03","title":"0.11.0 - 2026-04-03","text":""},{"location":"changelog/#breaking-changes","title":"Breaking Changes","text":"<ul> <li>\ud83d\udca5 Logging: split <code>caller</code> into separate <code>logger</code> (logger name) and <code>caller</code> (<code>function:line</code>) fields. <code>caller</code> is now opt-in via <code>LOG_CALLER_ENABLED</code> (default: <code>False</code>), following slog/zap/zerolog/Caddy conventions. Uvicorn formatter never includes <code>caller</code>.</li> </ul>"},{"location":"changelog/#features","title":"Features","text":"<ul> <li>\u2728 Add <code>LOG_CALLER_ENABLED</code> setting to opt in to caller info (<code>function:line</code>) in log records. Disabled by default for cleaner logs and better performance.</li> <li>\u2728 Add <code>logger</code> field (logger name, e.g., <code>myapp.api</code>) to all log records across all backends and formats.</li> </ul>"},{"location":"changelog/#0100-2026-04-02","title":"0.10.0 - 2026-04-02","text":""},{"location":"changelog/#features_1","title":"Features","text":"<ul> <li>\u2728 Add <code>RateLimiter</code> to the <code>resilience</code> module: Redis-backed sliding-window rate limiting using the GCRA algorithm. Includes <code>RateLimitResult</code> with fields mapping to IETF rate limit headers, weighted requests via <code>cost</code> parameter, and <code>RateLimitExceededError</code>.</li> </ul>"},{"location":"changelog/#removals","title":"Removals","text":"<ul> <li>\ud83d\uddd1\ufe0f Remove deprecated <code>UvicornJSONFormatter</code> and <code>UvicornAccessJSONFormatter</code>. Use <code>UvicornFormatter</code> and <code>UvicornAccessFormatter</code> instead (deprecated since 0.9.1).</li> </ul>"},{"location":"changelog/#ci","title":"CI","text":"<ul> <li>\u26a1 Migrate PyPI publishing from API token to OIDC trusted publishing.</li> </ul>"},{"location":"changelog/#091-2026-04-01","title":"0.9.1 - 2026-04-01","text":""},{"location":"changelog/#deprecations","title":"Deprecations","text":"<ul> <li>\ud83d\uddd1\ufe0f <code>UvicornJSONFormatter</code> and <code>UvicornAccessJSONFormatter</code> are deprecated. Use <code>UvicornFormatter</code> and <code>UvicornAccessFormatter</code> instead. The new formatters respect <code>LOG_FORMAT</code> instead of always producing JSON. Old names kept as aliases with <code>DeprecationWarning</code>.</li> </ul>"},{"location":"changelog/#090-2026-04-01","title":"0.9.0 - 2026-04-01","text":""},{"location":"changelog/#breaking-changes_1","title":"Breaking Changes","text":"<ul> <li>\ud83d\udca5 <code>LOG_FORMAT</code> default changed from <code>JSON</code> to <code>AUTO</code>. In production (non-TTY), behavior is identical (JSON output). In local dev (TTY), output switches to human-readable <code>TEXT</code> with colors. Set <code>LOG_FORMAT=JSON</code> explicitly to restore the previous default.</li> </ul>"},{"location":"changelog/#features_2","title":"Features","text":"<ul> <li>\u2728 Add <code>AUTO</code> log format (new default): detects TTY and selects <code>TEXT</code> (terminal) or <code>JSON</code> (piped/CI).</li> <li>\u2728 Add <code>LOGFMT</code> log format: key-value pairs following the logfmt convention, 30-40% smaller than JSON.</li> <li>\u2728 Add <code>PRETTY</code> log format: multi-line indented output with structured error rendering, inspired by Rust tracing Pretty.</li> <li>\u2728 Enhanced <code>TEXT</code> format: now includes extra context fields as <code>key=value</code> pairs and supports ANSI colors.</li> <li>\u2728 Add <code>NO_COLOR</code> / <code>FORCE_COLOR</code> environment variable support following no-color.org standard.</li> </ul>"},{"location":"changelog/#080-2026-04-01","title":"0.8.0 - 2026-04-01","text":""},{"location":"changelog/#breaking-changes_2","title":"Breaking Changes","text":"<ul> <li>\ud83d\udca5 Backend imports moved to submodules. Use <code>from grelmicro.sync.redis import RedisSyncBackend</code> instead of <code>from grelmicro.sync import RedisSyncBackend</code>. Same for all sync, cache, and logging backends. See Import Strategy.</li> </ul>"},{"location":"changelog/#features_3","title":"Features","text":"<ul> <li>\u2728 Add Uvicorn JSON formatters (<code>UvicornJSONFormatter</code>, <code>UvicornAccessJSONFormatter</code>) for structured logging via <code>dictConfig</code>.</li> </ul>"},{"location":"changelog/#070-2026-03-31","title":"0.7.0 - 2026-03-31","text":""},{"location":"changelog/#breaking-changes_3","title":"Breaking Changes","text":"<ul> <li>\ud83d\udca5 Logging JSON format redesigned to follow industry standards (slog, zap, zerolog):<ul> <li><code>logger</code> renamed to <code>caller</code></li> <li><code>thread</code> removed</li> <li><code>ctx</code> removed: extra fields are now flat at the top level</li> <li><code>exception</code> replaced by structured <code>error</code> object (<code>type</code>, <code>message</code>, <code>stack</code>)</li> </ul> </li> </ul>"},{"location":"changelog/#features_4","title":"Features","text":"<ul> <li>\u2728 Add <code>tracing</code> module with <code>@instrument</code> decorator, <code>span()</code> context manager, and <code>add_context()</code> for unified logging and OTel instrumentation (inspired by Rust's <code>tracing</code> crate).</li> </ul>"},{"location":"changelog/#performance","title":"Performance","text":"<ul> <li>\u26a1 Logging: Up to +23% throughput across all backends.</li> <li>\u26a1 Use <code>OrderedDict</code> for O(1) LRU operations in <code>TTLCache</code>.</li> </ul>"},{"location":"changelog/#refactors","title":"Refactors","text":"<ul> <li>\u267b\ufe0f Extract shared Redis config into <code>grelmicro/_redis.py</code>.</li> <li>\u267b\ufe0f Make <code>TTLCache</code> generic and add <code>Doc</code> annotations.</li> <li>\u267b\ufe0f Extract context stack into <code>grelmicro/_context.py</code> to decouple logging from tracing.</li> <li>\u267b\ufe0f Filter private (<code>_</code>-prefixed) attributes from stdlib JSON log output.</li> <li>\u267b\ufe0f Widen <code>@instrument(skip=...)</code> type from <code>set[str]</code> to <code>AbstractSet[str]</code>.</li> </ul>"},{"location":"changelog/#removals_1","title":"Removals","text":"<ul> <li>\ud83d\uddd1\ufe0f <code>Synchronization</code> protocol removed. Use <code>SyncPrimitive</code> instead (deprecated since 0.6.0).</li> <li>\ud83d\uddd1\ufe0f <code>ResilienceException</code> removed. Use <code>ResilienceError</code> instead (deprecated since 0.6.0).</li> <li>\ud83d\uddd1\ufe0f The <code>token</code> parameter on lock errors removed (deprecated since 0.6.0).</li> <li>\ud83d\uddd1\ufe0f The <code>sync</code> parameter on <code>interval()</code> removed (deprecated since 0.6.0).</li> <li>\ud83d\uddd1\ufe0f The <code>scheduled()</code> decorator removed (deprecated since 0.6.0).</li> </ul>"},{"location":"changelog/#060-2026-03-30","title":"0.6.0 - 2026-03-30","text":""},{"location":"changelog/#deprecations_1","title":"Deprecations","text":"<ul> <li>\ud83d\uddd1\ufe0f <code>Synchronization</code> protocol renamed to <code>SyncPrimitive</code>. The old name still works but emits a <code>DeprecationWarning</code>. Will be removed in 0.7.0.</li> <li>\ud83d\uddd1\ufe0f <code>ResilienceException</code> renamed to <code>ResilienceError</code>. The old name still works but emits a <code>DeprecationWarning</code>. Will be removed in 0.7.0.</li> <li>\ud83d\uddd1\ufe0f The <code>token</code> parameter on <code>LockAcquireError</code>, <code>LockReleaseError</code>, and <code>LockNotOwnedError</code> is deprecated. Tokens are no longer included in error messages for security. Will be removed in 0.7.0.</li> <li>\ud83d\uddd1\ufe0f The <code>sync</code> parameter on <code>interval()</code> for <code>TaskLock</code> and <code>LeaderElection</code> is deprecated. Use <code>max_lock_seconds</code> and <code>leader</code> parameters instead. Will be removed in 0.7.0.</li> <li>\ud83d\uddd1\ufe0f The <code>scheduled()</code> decorator is deprecated. Use <code>interval()</code> with <code>max_lock_seconds</code> or <code>leader</code> instead. Will be removed in 0.7.0.</li> </ul>"},{"location":"changelog/#features_5","title":"Features","text":"<ul> <li>\u2728 Add in-memory TTL cache with LRU eviction, per-key stampede protection, and <code>@cached</code> decorator.</li> <li>\u2728 Add <code>RedisCacheBackend</code> for distributed cache storage.</li> <li>\u2728 Add cache statistics via <code>CacheInfo</code> (hits, misses, evictions, stampedes).</li> <li>\u2728 Add Kubernetes sync backend using Lease resources (<code>pip install grelmicro[kubernetes]</code>).</li> <li>\u2728 Add SQLite sync backend for home lab and local testing (<code>pip install grelmicro[sqlite]</code>).</li> </ul>"},{"location":"changelog/#security","title":"Security","text":"<ul> <li>\ud83d\udd12\ufe0f Remove token values from lock error messages to prevent leaking in logs.</li> <li>\ud83d\udd12\ufe0f Upgrade <code>requests</code> to 2.33.0 (CVE fix in transitive dependency).</li> </ul>"},{"location":"changelog/#refactors_1","title":"Refactors","text":"<ul> <li>\u267b\ufe0f Unify error hierarchy under <code>GrelmicroError</code> base class. All module errors (<code>SyncError</code>, <code>ResilienceError</code>, <code>LoggingError</code>, <code>TaskError</code>, <code>CacheError</code>) now share a common base.</li> <li>\u267b\ufe0f Use server-side timestamps and native Lease fields in sync backends.</li> <li>\u267b\ufe0f Simplify token generation from UUID-based to string concatenation.</li> <li>\u267b\ufe0f Harden TaskLock token nonce and error handling.</li> </ul>"},{"location":"changelog/#internal","title":"Internal","text":"<ul> <li>\u2705 Achieve 100% library code coverage.</li> <li>\ud83d\udc9a Fix flaky integration test timeout in CI.</li> <li>\u2b06\ufe0f Bump dependencies and fix ty v0.0.26 type errors.</li> </ul>"},{"location":"changelog/#docs","title":"Docs","text":"<ul> <li>\ud83d\udcdd Add cache module documentation with usage guide and API reference.</li> <li>\ud83d\udcdd Add Kubernetes Backend Architecture page.</li> <li>\ud83d\udcdd Add SQLite Backend Architecture page.</li> <li>\ud83d\udcdd Add backend comparison matrix to Synchronization guide.</li> <li>\ud83d\udcdd Rewrite README with project vision.</li> </ul>"},{"location":"changelog/#050-2026-03-17","title":"0.5.0 - 2026-03-17","text":""},{"location":"changelog/#breaking-changes_4","title":"Breaking Changes","text":"<ul> <li>\ud83d\udca5 Add namespace prefix to sync primitive backend keys (<code>lock:</code>, <code>tasklock:</code>, <code>leader:</code>). See Migration Guide below.</li> </ul>"},{"location":"changelog/#features_6","title":"Features","text":"<ul> <li>\u2728 Add <code>TaskLock.from_thread</code> thread-safe adapter. PR #57.</li> <li>\u2728 Add specific lock error classes (<code>LockAcquireError</code>, <code>LockReleaseError</code>, <code>LockLockedCheckError</code>, <code>LockOwnedCheckError</code>, <code>LockReentrantError</code>). PR #57.</li> </ul>"},{"location":"changelog/#refactors_2","title":"Refactors","text":"<ul> <li>\u267b\ufe0f Consolidate distributed lock and leader gating into the <code>interval()</code> decorator via <code>max_lock_seconds</code> and <code>leader</code> parameters. PR #54.</li> </ul>"},{"location":"changelog/#docs_1","title":"Docs","text":"<ul> <li>\ud83d\udcdd Add Synchronization Architecture page. PR #57.</li> </ul>"},{"location":"changelog/#internal_1","title":"Internal","text":"<ul> <li>\u2b06\ufe0f Bump redis, fastapi, pydantic, and pydantic-settings. PR #55.</li> <li>\u2b06\ufe0f Update pre-commit hooks. PR #50.</li> </ul>"},{"location":"changelog/#migration-guide","title":"Migration Guide","text":""},{"location":"changelog/#namespace-prefixed-backend-keys","title":"Namespace-Prefixed Backend Keys","text":"<p>Prior versions used the <code>name</code> parameter directly as the backend key. Now each primitive adds a type-specific prefix:</p> Primitive Name Backend Key <code>Lock(\"my-resource\")</code> <code>my-resource</code> <code>lock:my-resource</code> <code>TaskLock(\"cleanup\")</code> <code>cleanup</code> <code>tasklock:cleanup</code> <code>LeaderElection(\"main\")</code> <code>main</code> <code>leader:main</code> <p>Existing locks stored in Redis or PostgreSQL will no longer match after upgrading. A running instance on the old version and one on the new version will not see each other's locks.</p> <p>Upgrade all running instances together so they use the same key format. Old keys expire automatically via their lease duration (Redis <code>PEXPIRE</code> / PostgreSQL <code>expire_at</code>).</p>"},{"location":"changelog/#041-2026-03-13","title":"0.4.1 - 2026-03-13","text":""},{"location":"changelog/#docs_2","title":"Docs","text":"<ul> <li>\ud83d\udcdd Add Task Lock to synchronization primitives guide.</li> </ul>"},{"location":"changelog/#internal_2","title":"Internal","text":"<ul> <li>\u2b06\ufe0f Bump actions/checkout to v6 and astral-sh/setup-uv to v7.</li> </ul>"},{"location":"changelog/#040-2026-03-13","title":"0.4.0 - 2026-03-13","text":""},{"location":"changelog/#features_7","title":"Features","text":"<ul> <li>\u2728 Add <code>TaskLock</code> for ShedLock-style distributed task locking.</li> <li>\u2728 Add <code>LOG_TIMEZONE</code> support for configurable timezone in logging output.</li> <li>\u2728 Add OpenTelemetry trace context injection into log records.</li> <li>\u2728 Add <code>structlog</code> as alternative logging backend.</li> <li>\u2728 Add configurable JSON serializer (<code>json</code> / <code>orjson</code>) for logging.</li> </ul>"},{"location":"changelog/#docs_3","title":"Docs","text":"<ul> <li>\ud83d\udcdd Add logging benchmark and performance documentation.</li> </ul>"},{"location":"changelog/#internal_3","title":"Internal","text":"<ul> <li>\u2b06\ufe0f Bump orjson from 3.11.5 to 3.11.6. PR #51.</li> <li>\u2b06\ufe0f Bump freezegun from 1.5.2 to 1.5.5. PR #33.</li> </ul>"},{"location":"changelog/#032-2026-01-27","title":"0.3.2 - 2026-01-27","text":""},{"location":"changelog/#internal_4","title":"Internal","text":"<ul> <li>\ud83d\udc77 Migrate from mypy to Astral ty for type checking. PR #45.</li> <li>\ud83d\udd27 Add Python 3.14 support. PR #47.</li> <li>\ud83d\udd27 Switch build system to <code>uv_build</code>. PR #49.</li> <li>\ud83d\udc9a Simplify CI and release workflow. PR #24.</li> </ul>"},{"location":"changelog/#031-2025-06-05","title":"0.3.1 - 2025-06-05","text":""},{"location":"changelog/#docs_4","title":"Docs","text":"<ul> <li>\ud83d\udcdd Add resilience patterns section and update links in README and index.</li> </ul>"},{"location":"changelog/#internal_5","title":"Internal","text":"<ul> <li>\ud83d\udc9a Fix release pipeline and GitHub Pages deployment permissions.</li> </ul>"},{"location":"changelog/#030-2025-06-05","title":"0.3.0 - 2025-06-05","text":""},{"location":"changelog/#features_8","title":"Features","text":"<ul> <li>\u2728 Add Circuit Breaker resilience pattern. PR #18.</li> </ul>"},{"location":"changelog/#docs_5","title":"Docs","text":"<ul> <li>\ud83d\udcdd Refactor code examples to use snippets.</li> </ul>"},{"location":"changelog/#internal_6","title":"Internal","text":"<ul> <li>\ud83d\udc77 Add Dependabot configuration for weekly updates.</li> <li>\ud83d\udd12\ufe0f Fix workflow permission issues. PR #21, #22.</li> </ul>"},{"location":"changelog/#023-2024-12-04","title":"0.2.3 - 2024-12-04","text":""},{"location":"changelog/#features_9","title":"Features","text":"<ul> <li>\u2728 Add Redis key prefix support to avoid conflicts in shared instances.</li> <li>\u2728 Add Redis and PostgreSQL settings management from environment variables.</li> </ul>"},{"location":"changelog/#022-2024-11-28","title":"0.2.2 - 2024-11-28","text":""},{"location":"changelog/#features_10","title":"Features","text":"<ul> <li>\u2728 Add PostgreSQL backend configuration from environment variables.</li> </ul>"},{"location":"changelog/#internal_7","title":"Internal","text":"<ul> <li>\ud83d\udc1b Fix release workflow. PR #7, #9.</li> </ul>"},{"location":"changelog/#021-2024-11-26","title":"0.2.1 - 2024-11-26","text":""},{"location":"changelog/#internal_8","title":"Internal","text":"<ul> <li>\ud83d\udc9a Set up release workflow with version tagging.</li> </ul>"},{"location":"changelog/#020-2024-11-26","title":"0.2.0 - 2024-11-26","text":"<p>First public release.</p>"},{"location":"changelog/#features_11","title":"Features","text":"<ul> <li>\u2728 Add distributed <code>Lock</code> with lease-based expiration.</li> <li>\u2728 Add <code>LeaderElection</code> for single-leader task execution.</li> <li>\u2728 Add <code>IntervalTask</code> scheduler for periodic tasks with synchronization support.</li> <li>\u2728 Add Redis, PostgreSQL, and in-memory synchronization backends.</li> <li>\u2728 Add logging module with JSON and TEXT formatting via <code>LOG_LEVEL</code> and <code>LOG_FORMAT</code> environment variables.</li> </ul>"},{"location":"changelog/#docs_6","title":"Docs","text":"<ul> <li>\ud83d\udcdd Add MkDocs documentation site with Material theme.</li> </ul>"},{"location":"changelog/#internal_9","title":"Internal","text":"<ul> <li>\ud83d\udc77 Add unified CI workflow with linting, testing, and coverage.</li> </ul>"},{"location":"logging/","title":"Logging","text":"<p>The <code>logging</code> package provides a simple, zero-config logging system following the 12-factor app methodology.</p> <p>Logs go to stdout. Format is selected automatically. Configuration is done via environment variables.</p>"},{"location":"logging/#quick-start","title":"Quick Start","text":"<pre><code>from grelmicro.logging import configure_logging\n\nconfigure_logging()\n</code></pre> <p>With no environment variables set, <code>configure_logging()</code> detects your terminal:</p> <ul> <li>Terminal (TTY): human-readable colored text</li> <li>Piped / CI / container: structured JSON</li> </ul> <p>This is the <code>AUTO</code> format (the default).</p>"},{"location":"logging/#backend-selection","title":"Backend Selection","text":"<p>grelmicro supports three logging backends. All backends produce identical output for each format, making it easy to switch.</p> Backend Dependencies Best for stdlib (default) None Zero-dependency setups Loguru <code>loguru</code> Developer ergonomics structlog <code>structlog</code> High-throughput services"},{"location":"logging/#installation","title":"Installation","text":"stdlib (no dependencies)LogurustructlogWith OpenTelemetry <p>No additional dependencies required. Uses Python's built-in <code>logging</code> module.</p> <pre><code>pip install grelmicro[standard]\n</code></pre> <pre><code>pip install grelmicro[structlog]\n</code></pre> <pre><code>pip install grelmicro[standard,opentelemetry]\n# or\npip install grelmicro[structlog,opentelemetry]\n</code></pre>"},{"location":"logging/#usage","title":"Usage","text":"<p>Select the backend with the <code>LOG_BACKEND</code> environment variable, then use the corresponding logger:</p> Logurustructlogstdlib <pre><code>from loguru import logger\n\nconfigure_logging()\nlogger.info(\"Hello, World!\", user_id=123)\n</code></pre> <pre><code>import structlog\n\nconfigure_logging()\nlog = structlog.get_logger()\nlog.info(\"Hello, World!\", user_id=123)\n</code></pre> <pre><code>import logging\n\nconfigure_logging()\nlogger = logging.getLogger(__name__)\nlogger.info(\"Hello, World!\", extra={\"user_id\": 123})\n</code></pre>"},{"location":"logging/#log-formats","title":"Log Formats","text":"<p>grelmicro provides five format options, inspired by the Rust tracing ecosystem and Go slog:</p> Format Use Case Machine-Parseable Inspired By <code>AUTO</code> Default. Adapts to environment Depends zerolog, pino <code>JSON</code> Production, log aggregation Yes pino, tracing JSON, slog JSON <code>LOGFMT</code> Structured + human-readable Yes Go slog TextHandler, tracing-logfmt <code>TEXT</code> Local development No tracing Full, loguru <code>PRETTY</code> Verbose debugging No tracing Pretty, structlog ConsoleRenderer"},{"location":"logging/#auto-default","title":"AUTO (Default)","text":"<p>Detects the output target and selects the best format automatically:</p> Condition Selected Format <code>stdout</code> is a TTY (terminal) <code>TEXT</code> (colored) <code>stdout</code> is piped or redirected <code>JSON</code> <code>FORCE_COLOR</code> env var set <code>TEXT</code> (colored) <code>NO_COLOR</code> env var set <code>JSON</code> <pre><code>\"\"\"Example: AUTO format logging (default).\"\"\"\n\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\nlogger.remove()\n\n# AUTO is the default: TEXT in terminal, JSON when piped.\n# No LOG_FORMAT env var needed.\nconfigure_logging()\n\nlogger.info(\"Application started\", version=\"1.0.0\")\n</code></pre> <p>In your terminal: <pre><code>2026-04-01 10:30:00.123 INFO     __main__ - Application started version=1.0.0\n</code></pre></p> <p>In a container or CI: <pre><code>{\"time\":\"2026-04-01T08:30:00.123456+00:00\",\"level\":\"INFO\",\"msg\":\"Application started\",\"logger\":\"__main__\",\"version\":\"1.0.0\"}\n</code></pre></p> <p>Zero Config</p> <p><code>AUTO</code> is the default. Most users never need to set <code>LOG_FORMAT</code>.</p>"},{"location":"logging/#json","title":"JSON","text":"<p>Structured newline-delimited JSON. Ideal for production, log aggregation (Datadog, Loki, ELK).</p> <pre><code>LOG_FORMAT=JSON\n</code></pre> <pre><code>\"\"\"Example: JSON format logging with timezone.\"\"\"\n\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\n# Ensure clean state\nlogger.remove()\n\nconfigure_logging()\n\nlogger.info(\"Application started\", version=\"1.0.0\", environment=\"production\")\n</code></pre> <p>Output: <pre><code>{\"time\":\"2026-04-01T10:30:00.123456+02:00\",\"level\":\"INFO\",\"msg\":\"Application started\",\"logger\":\"__main__\",\"version\":\"1.0.0\",\"environment\":\"production\"}\n</code></pre></p>"},{"location":"logging/#logfmt","title":"LOGFMT","text":"<p>Key-value pairs following the logfmt convention. 30-40% smaller than JSON, grep-friendly, parseable by Grafana Loki and most log tools.</p> <pre><code>LOG_FORMAT=LOGFMT\n</code></pre> <pre><code>\"\"\"Example: LOGFMT format logging.\"\"\"\n\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\nlogger.remove()\n\nconfigure_logging()\n\nlogger.info(\"Request handled\", method=\"GET\", path=\"/health\", status=200)\n</code></pre> <p>Output: <pre><code>time=2026-04-01T10:30:00.123456+00:00 level=INFO msg=\"Request handled\" logger=__main__ method=GET path=/health status=200\n</code></pre></p> <p>Nested dicts use dot notation: <pre><code>error.type=ValueError error.message=\"invalid input\"\n</code></pre></p>"},{"location":"logging/#text","title":"TEXT","text":"<p>Single-line, human-readable output. Includes extra fields as <code>key=value</code> pairs. Colors are enabled when output is a TTY.</p> <pre><code>LOG_FORMAT=TEXT\n</code></pre> <pre><code>\"\"\"Example: TEXT format logging with timezone.\"\"\"\n\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\n# Ensure clean state\nlogger.remove()\n\nconfigure_logging()\n\nlogger.info(\"Application started\", version=\"1.0.0\")\n</code></pre> <p>Output: <pre><code>2026-04-01 10:30:00.123 INFO     __main__:&lt;module&gt;:12 - Application started version=1.0.0\n</code></pre></p>"},{"location":"logging/#pretty","title":"PRETTY","text":"<p>Multi-line format with indented fields. Best for debugging with low log volume.</p> <pre><code>LOG_FORMAT=PRETTY\n</code></pre> <pre><code>\"\"\"Example: PRETTY format logging.\"\"\"\n\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\nlogger.remove()\n\nconfigure_logging()\n\nlogger.info(\"Request handled\", method=\"GET\", path=\"/health\", status=200)\n</code></pre> <p>Output: <pre><code>  2026-04-01 10:30:00.123 INFO Request handled\n    at __main__:&lt;module&gt;:10\n    method: GET\n    path: /health\n    status: 200\n</code></pre></p> <p>With exceptions: <pre><code>  2026-04-01 10:30:01.456 ERROR Operation failed\n    at myapp.service:process:78\n    error.type: ZeroDivisionError\n    error.message: division by zero\n    error.stack:\n      Traceback (most recent call last):\n        File \"service.py\", line 78, in process\n          result = 1 / 0\n      ZeroDivisionError: division by zero\n</code></pre></p>"},{"location":"logging/#settings","title":"Settings","text":"<p>All configuration is done via environment variables:</p> Variable Values Default <code>LOG_BACKEND</code> <code>stdlib</code>, <code>loguru</code>, <code>structlog</code> <code>stdlib</code> <code>LOG_LEVEL</code> <code>DEBUG</code>, <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code>, <code>CRITICAL</code> <code>INFO</code> <code>LOG_FORMAT</code> <code>AUTO</code>, <code>JSON</code>, <code>LOGFMT</code>, <code>TEXT</code>, <code>PRETTY</code> <code>AUTO</code> <code>LOG_TIMEZONE</code> IANA timezone (e.g., <code>UTC</code>, <code>Europe/Zurich</code>) <code>UTC</code> <code>LOG_JSON_SERIALIZER</code> <code>stdlib</code>, <code>orjson</code> <code>stdlib</code> <code>LOG_CALLER_ENABLED</code> <code>true</code>, <code>false</code> <code>false</code> <code>LOG_OTEL_ENABLED</code> <code>true</code>, <code>false</code> auto-detected <code>NO_COLOR</code> any value (unset) <code>FORCE_COLOR</code> any value (unset) <p>Color Support</p> <p>Colors follow the NO_COLOR and FORCE_COLOR standards. When <code>NO_COLOR</code> is set, <code>AUTO</code> resolves to <code>JSON</code> and colors are disabled. <code>FORCE_COLOR</code> takes precedence over <code>NO_COLOR</code>.</p>"},{"location":"logging/#timezone","title":"Timezone","text":"<p>The <code>LOG_TIMEZONE</code> setting controls timestamps in all formats:</p> <pre><code>LOG_TIMEZONE=Europe/Zurich\n</code></pre> <p>JSON / LOGFMT: ISO 8601 with timezone offset <pre><code>\"time\":\"2026-04-01T15:56:36.066922+01:00\"\n</code></pre></p> <p>TEXT / PRETTY: Localized time <pre><code>2026-04-01 15:56:36.066\n</code></pre></p>"},{"location":"logging/#structured-logging","title":"Structured Logging","text":"<p>Extra context fields are passed as keyword arguments and appear as flat top-level fields:</p> <pre><code>\"\"\"Example: Structured logging with context.\"\"\"\n\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\n# Ensure clean state\nlogger.remove()\n\nconfigure_logging()\n\nlogger.info(\"User logged in\", user_id=123, ip_address=\"192.168.1.1\")\n</code></pre> <p>Output: <pre><code>{\"time\":\"...\",\"level\":\"INFO\",\"msg\":\"User logged in\",\"logger\":\"...\",\"user_id\":123,\"ip_address\":\"192.168.1.1\"}\n</code></pre></p>"},{"location":"logging/#exception-handling","title":"Exception Handling","text":"<p>Exceptions are automatically captured as structured <code>ErrorDict</code>:</p> <pre><code>\"\"\"Example: Exception logging with context.\"\"\"\n\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\n# Ensure clean state\nlogger.remove()\n\nconfigure_logging()\n\ntry:\n    1 / 0  # noqa: B018\nexcept ZeroDivisionError:\n    logger.exception(\"Operation failed\", operation=\"divide\")\n</code></pre> <p>JSON output: <pre><code>{\"time\":\"...\",\"level\":\"ERROR\",\"msg\":\"Operation failed\",\"logger\":\"...\",\"operation\":\"divide\",\"error\":{\"type\":\"ZeroDivisionError\",\"message\":\"division by zero\",\"stack\":\"...\"}}\n</code></pre></p> <p>LOGFMT output: <pre><code>time=... level=ERROR msg=\"Operation failed\" logger=... error.type=ZeroDivisionError error.message=\"division by zero\" error.stack=\"Traceback...\"\n</code></pre></p> <p>PRETTY output: <pre><code>  ... ERROR Operation failed\n    at ...\n    operation: divide\n    error.type: ZeroDivisionError\n    error.message: division by zero\n    error.stack:\n      Traceback (most recent call last):\n        ...\n      ZeroDivisionError: division by zero\n</code></pre></p>"},{"location":"logging/#opentelemetry-integration","title":"OpenTelemetry Integration","text":"<p>When OpenTelemetry is installed, <code>trace_id</code> and <code>span_id</code> are automatically added to logs:</p> <pre><code>\"\"\"OpenTelemetry integration example.\"\"\"\n\nfrom loguru import logger\nfrom opentelemetry import trace\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import (\n    ConsoleSpanExporter,\n    SimpleSpanProcessor,\n)\n\nfrom grelmicro.logging import configure_logging\n\n# Set up OpenTelemetry\nprovider = TracerProvider()\nprocessor = SimpleSpanProcessor(ConsoleSpanExporter())\nprovider.add_span_processor(processor)\ntrace.set_tracer_provider(provider)\n\n# Configure logging (auto-detects OpenTelemetry)\nconfigure_logging()\n\n# Get a tracer\ntracer = trace.get_tracer(__name__)\n\n# Logs inside spans will automatically include trace_id and span_id\nwith tracer.start_as_current_span(\"handle_request\") as span:\n    logger.info(\"Processing request\", user_id=123, endpoint=\"/api/users\")\n\n    with tracer.start_as_current_span(\"database_query\"):\n        logger.info(\"Executing query\", query=\"SELECT * FROM users\")\n\n    logger.info(\"Request completed\", status=\"success\")\n</code></pre> <p>Output: <pre><code>{\n  \"time\": \"2026-01-27T16:00:00.000Z\",\n  \"level\": \"INFO\",\n  \"msg\": \"Processing request\",\n  \"logger\": \"myapp.service\",\n  \"trace_id\": \"4bf92f3577b34da6a3ce929d0e0e4736\",\n  \"span_id\": \"00f067aa0ba902b7\",\n  \"user_id\": 123\n}\n</code></pre></p> <p>Trace fields follow the OpenTelemetry standard and are placed at the JSON root level for compatibility with observability platforms (Jaeger, Zipkin, DataDog, Grafana Tempo).</p> <p>To disable: <code>LOG_OTEL_ENABLED=false</code></p>"},{"location":"logging/#fastapi-integration","title":"FastAPI Integration","text":"<pre><code>from contextlib import asynccontextmanager\n\nfrom fastapi import FastAPI\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\n\n@asynccontextmanager\ndef lifespan_startup():\n    # Ensure logging is configured during startup\n    configure_logging()\n    yield\n\n\napp = FastAPI()\n\n\n@app.get(\"/\")\ndef root():\n    logger.info(\"This is an info message\")\n    return {\"Hello\": \"World\"}\n</code></pre> <p>Warning</p> <p>Call <code>configure_logging</code> during the lifespan of the FastAPI application. The FastAPI CLI may reset the logging configuration otherwise.</p>"},{"location":"logging/#uvicorn-integration","title":"Uvicorn Integration","text":"<p>Uvicorn has its own logging system separate from your application. To get consistent output between uvicorn and your app, use the format-aware uvicorn formatters via a log config file:</p> <pre><code>{\n  \"version\": 1,\n  \"disable_existing_loggers\": false,\n  \"formatters\": {\n    \"default\": {\n      \"()\": \"grelmicro.logging.uvicorn.UvicornFormatter\"\n    },\n    \"access\": {\n      \"()\": \"grelmicro.logging.uvicorn.UvicornAccessFormatter\"\n    }\n  },\n  \"handlers\": {\n    \"default\": {\n      \"class\": \"logging.StreamHandler\",\n      \"formatter\": \"default\",\n      \"stream\": \"ext://sys.stdout\"\n    },\n    \"access\": {\n      \"class\": \"logging.StreamHandler\",\n      \"formatter\": \"access\",\n      \"stream\": \"ext://sys.stdout\"\n    }\n  },\n  \"loggers\": {\n    \"uvicorn\": {\n      \"handlers\": [\"default\"],\n      \"level\": \"INFO\",\n      \"propagate\": false\n    },\n    \"uvicorn.access\": {\n      \"handlers\": [\"access\"],\n      \"level\": \"INFO\",\n      \"propagate\": false\n    }\n  }\n}\n</code></pre> <p>Then start uvicorn with:</p> <pre><code>uvicorn app:app --log-config uvicorn_log_config.json\n</code></pre> <p><code>UvicornFormatter</code> and <code>UvicornAccessFormatter</code> read <code>LOG_FORMAT</code> at startup and produce the matching output (AUTO, JSON, LOGFMT, TEXT, PRETTY). This ensures uvicorn logs and application logs use the same format.</p> <p><code>UvicornAccessFormatter</code> additionally parses uvicorn's access log arguments into structured fields: <code>client_addr</code>, <code>method</code>, <code>full_path</code>, <code>http_version</code>, <code>status_code</code>.</p>"},{"location":"logging/#custom-format-loguru-only","title":"Custom Format (Loguru only)","text":"<p>You can provide a custom loguru format template:</p> <pre><code>LOG_FORMAT=\"{level} | {message}\"\n</code></pre> <pre><code>\"\"\"Example: Custom format logging.\"\"\"\n\nfrom loguru import logger\n\nfrom grelmicro.logging import configure_logging\n\n# Ensure clean state\nlogger.remove()\n\nconfigure_logging()\n\nlogger.info(\"Custom format example\")\n</code></pre> <p>Output: <pre><code>INFO | Custom format example\n</code></pre></p> <p>Note</p> <p>Custom format strings only work with the loguru backend.</p>"},{"location":"logging/#json-record-structure","title":"JSON Record Structure","text":"<p>All JSON log records follow this schema. Required fields are always present, optional fields may be absent. Extra context fields are merged flat at the top level:</p> <pre><code>class JSONRecordDict:\n    # Required\n    time: str              # ISO 8601 timestamp with timezone\n    level: str             # DEBUG, INFO, WARNING, ERROR, CRITICAL\n    msg: str               # Log message\n    logger: str            # Logger name (e.g., \"myapp.api\")\n    # Optional (opt-in via LOG_CALLER_ENABLED=true)\n    caller: str            # function:line (e.g., \"handle:45\")\n    # Optional\n    trace_id: str          # OpenTelemetry trace ID (32 hex chars)\n    span_id: str           # OpenTelemetry span ID (16 hex chars)\n    error: ErrorDict       # Structured error info\n</code></pre> <p>The <code>ErrorDict</code> structure:</p> <pre><code>class ErrorDict:\n    type: str              # Exception class name (e.g., \"ValueError\")\n    message: str           # Exception message\n    stack: str             # Optional: full traceback string\n</code></pre>"},{"location":"logging/#design-decisions","title":"Design Decisions","text":"<p>Level casing: UPPERCASE (<code>DEBUG</code>, <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code>, <code>CRITICAL</code>), following Go slog, Java Log4j2, Rust tracing conventions.</p> <p>Field naming: Core field names (<code>time</code>, <code>level</code>, <code>msg</code>, <code>logger</code>, <code>caller</code>, <code>error</code>) follow slog/zap/Caddy conventions. <code>logger</code> is the logger name; <code>caller</code> is the call site (<code>function:line</code>).</p> <p>Caller opt-in: <code>caller</code> is disabled by default, following slog/zap/zerolog/Caddy conventions where caller info is opt-in. Enable with <code>LOG_CALLER_ENABLED=true</code>. Uvicorn formatters never include <code>caller</code> (points to uvicorn internals, not application code).</p> <p>Collision protection: Core fields cannot be overwritten by user-supplied extra context.</p>"},{"location":"logging/#production-deployment","title":"Production Deployment","text":"<p>For strict unbuffered output (12-factor compliance):</p> <pre><code>PYTHONUNBUFFERED=1\n</code></pre>"},{"location":"logging/#performance","title":"Performance","text":"<p>Benchmark results (50,000 iterations):</p> Backend Serializer Ops/sec vs Best structlog orjson 302,273 100.0% stdlib orjson 269,353 89.1% structlog stdlib 198,000 65.5% loguru orjson 192,953 63.8% stdlib stdlib 181,745 60.1% loguru stdlib 147,185 48.7% <p>Performance Recommendation</p> <p>For high-throughput applications, use <code>LOG_JSON_SERIALIZER=orjson</code> with <code>structlog</code> or <code>stdlib</code> backend.</p> <p>Run the benchmark: <pre><code>python benchmarks/logging_benchmark.py\n</code></pre></p>"},{"location":"resilience/","title":"Resilience Patterns","text":"<p>The <code>resilience</code> package provides patterns to improve fault tolerance and reliability in distributed systems.</p> <ul> <li>Circuit Breaker: Automatically detects repeated failures and temporarily blocks calls to unstable services, allowing them time to recover.</li> <li>Rate Limiter: Limits the number of requests per time window to protect services from overload.</li> </ul>"},{"location":"resilience/#circuit-breaker","title":"Circuit Breaker","text":"<p>A Circuit Breaker prevents repeated failures when calling unreliable services. It monitors call outcomes and, after too many consecutive failures, \"opens\" to block further calls for a period, allowing recovery.</p> <p>Why Circuit Breakers?</p> <ul> <li>Prevent cascading failures</li> <li>Improve stability and user experience</li> <li>Provide observability into service health</li> </ul>"},{"location":"resilience/#state-machine","title":"State Machine","text":"<p>The Circuit Breaker has three normal states and two manual (forced) states:</p> State Description CLOSED Normal operation, calls are allowed. OPEN Calls are blocked to allow recovery. HALF_OPEN Allows limited calls to test if the service has recovered. FORCED_OPEN Manual state to block calls regardless of health checks. FORCED_CLOSED Manual state to allow calls regardless of health checks."},{"location":"resilience/#usage","title":"Usage","text":"<pre><code>from grelmicro.resilience.circuitbreaker import CircuitBreaker\n\ncircuit_breaker = CircuitBreaker(\n    \"system_name\", ignore_exceptions=FileNotFoundError\n)\n\n\nasync def async_context_manager():\n    async with circuit_breaker:\n        print(\"Calling external service...\")\n\n\n@circuit_breaker\nasync def async_call():\n    print(\"Calling external service...\")\n\n\ndef sync_context_manager():\n    with circuit_breaker.from_thread:\n        print(\"Calling external service from AnyIO worker thread...\")\n\n\n@circuit_breaker\ndef sync_call():\n    print(\"Calling external service from AnyIO worker thread...\")\n</code></pre> <p>Warning</p> <p>Thread Safety: The Circuit Breaker is not thread-safe. Decorated sync functions or <code>from_thread</code> methods will ensure state change logic runs safely within the async event loop. Threaded usage is supported only in AnyIO worker threads and may be slower than pure async usage.</p>"},{"location":"resilience/#rate-limiter","title":"Rate Limiter","text":"<p>A Rate Limiter controls how many requests a client can make within a time window. It uses the GCRA (Generic Cell Rate Algorithm), which is memory-efficient (~72 bytes per key) and provides exact accuracy with no window boundary issues.</p> <p>Why Rate Limiting?</p> <ul> <li>Protect services from overload and abuse</li> <li>Enforce fair usage across clients</li> <li>Enable HTTP 429 responses with standard headers</li> </ul>"},{"location":"resilience/#backend","title":"Backend","text":"<p>You must load a rate limiter backend before using the rate limiter.</p> RedisMemory <pre><code>from grelmicro.resilience.redis import RedisRateLimiterBackend\n\nbackend = RedisRateLimiterBackend(\"redis://localhost:6379/0\")\n</code></pre> <pre><code>from grelmicro.resilience.memory import MemoryRateLimiterBackend\n\nbackend = MemoryRateLimiterBackend()\n</code></pre> <p>Warning</p> <p>Please make sure to use a proper way to store connection URLs, such as environment variables (not like the example above).</p> Redis Memory Use case Production Testing only Multi-node Yes No Persistence Yes (auto-expiring keys) No"},{"location":"resilience/#usage_1","title":"Usage","text":"<pre><code>from grelmicro.resilience.ratelimiter import RateLimiter\n\n# Create rate limiters for different concerns\nauth_limiter = RateLimiter(\"auth\", limit=5, window=60)\napi_limiter = RateLimiter(\"api\", limit=100, window=60)\n\n\nasync def login(ip: str) -&gt; None:\n    result = await auth_limiter.acquire(key=ip)\n    if not result.allowed:\n        print(f\"Too many attempts, retry after {result.retry_after:.0f}s\")\n        return\n    print(f\"Login allowed, {result.remaining} attempts remaining\")\n\n\nasync def api_call(user_id: str) -&gt; None:\n    # Raises RateLimitExceededError if limit exceeded\n    await api_limiter.acquire_or_raise(key=user_id)\n    print(\"API call allowed\")\n</code></pre>"},{"location":"resilience/#result-fields","title":"Result Fields","text":"<p>The <code>RateLimitResult</code> provides all information needed for HTTP rate limit headers:</p> Field Type Description HTTP Header <code>allowed</code> <code>bool</code> Whether the request is permitted 200 vs 429 status <code>limit</code> <code>int</code> Total quota for the window <code>X-RateLimit-Limit</code> <code>remaining</code> <code>int</code> Remaining requests in the window <code>X-RateLimit-Remaining</code> <code>retry_after</code> <code>float</code> Seconds until next allowed request <code>Retry-After</code> <code>reset_after</code> <code>float</code> Seconds until full quota resets <code>X-RateLimit-Reset</code>"},{"location":"resilience/#weighted-requests","title":"Weighted Requests","text":"<p>Use the <code>cost</code> parameter to consume multiple tokens per request:</p> <pre><code># Bulk endpoint costs 10 tokens\nresult = await api_limiter.acquire(key=user_id, cost=10)\n</code></pre> <p>Tip</p> <p>The rate limiter uses the same backend registry pattern as the synchronization primitives. See Backend Architecture for details.</p>"},{"location":"sync/","title":"Synchronization Primitives","text":"<p>The <code>sync</code> package provides synchronization primitives for distributed systems.</p> <p>The primitives are technology agnostic, supporting multiple backends (see more in the Backends section).</p> <p>The available primitives are:</p> <ul> <li>Task Lock: A distributed lock for scheduled tasks with minimum and maximum hold times.</li> <li>Leader Election: A single worker is elected as the leader for performing tasks only once in a cluster.</li> <li>Lock: A distributed lock that can be used to synchronize access to shared resources.</li> </ul> <p>The synchronization primitives can be used in combination with the <code>TaskManager</code> and <code>TaskRouter</code> to control task execution in a distributed system (see more in Task Scheduler).</p> <p>Thread Safety</p> <p>All synchronization primitives (<code>Lock</code>, <code>TaskLock</code>, <code>LeaderElection</code>) are designed for use within a single async event loop and are not thread-safe. Sync access from worker threads is supported via <code>from_thread</code> adapters, which dispatch operations to the event loop. Do not share instances across multiple event loops or threads without the adapter.</p>"},{"location":"sync/#backend","title":"Backend","text":"<p>You must load a synchronization backend before using synchronization primitives.</p> <p>Note</p> <p>Although grelmicro uses AnyIO for concurrency, the backends generally depend on <code>asyncio</code>, therefore Trio is not supported.</p> <p>You can initialize a backend like this:</p> RedisPostgresKubernetesSQLiteMemory <pre><code>from grelmicro.sync.redis import RedisSyncBackend\n\nbackend = RedisSyncBackend(\"redis://localhost:6379/0\")\n</code></pre> <pre><code>from grelmicro.sync.postgres import PostgresSyncBackend\n\nbackend = PostgresSyncBackend(\"postgresql://user:password@localhost:5432/db\")\n</code></pre> <pre><code>from grelmicro.sync.kubernetes import KubernetesSyncBackend\n\nbackend = KubernetesSyncBackend(namespace=\"default\")\n</code></pre> <pre><code>from grelmicro.sync.sqlite import SQLiteSyncBackend\n\nbackend = SQLiteSyncBackend(\"locks.db\")\n</code></pre> <pre><code>from grelmicro.sync.memory import MemorySyncBackend\n\nbackend = MemorySyncBackend()\n</code></pre> <p>Warning</p> <p>Please make sure to use a proper way to store connection URLs, such as environment variables (not like the example above).</p> Redis PostgreSQL Kubernetes SQLite Memory Use case Production Production Production (K8s-native) Home lab / Local testing Testing only Multi-node Yes Yes Yes No No Persistence Yes Yes Yes (etcd-backed) Yes No Extra infrastructure Required None if already in stack None (uses existing K8s API) None None Lock performance Best Good Moderate Good Best <p>Tip</p> <p>Feel free to create your own backend and contribute it. In the <code>sync.abc</code> module, you can find the protocol for creating new backends.</p>"},{"location":"sync/#task-lock","title":"Task Lock","text":"<p>The Task Lock is a distributed lock designed for scheduled tasks. Unlike a regular Lock, it does not release immediately. Instead, it keeps the lock held for a configurable minimum duration to prevent re-execution on other nodes.</p> <p>There is no background task that maintains the lock active during execution. The lock relies entirely on the TTL (<code>max_lock_seconds</code>) set at acquire time. If the task runs longer than <code>max_lock_seconds</code>, the lock expires and another node may acquire it.</p> <ul> <li><code>min_lock_seconds</code>: Minimum duration to hold the lock after task completion. Prevents another node from re-executing too soon.</li> <li><code>max_lock_seconds</code>: Maximum duration to hold the lock. Acts as a TTL for crash/deadlock protection.</li> </ul> <p>Tip</p> <p>For scheduled tasks, prefer the <code>interval()</code> decorator with <code>max_lock_seconds</code> which configures a <code>TaskLock</code> automatically with sensible defaults.</p> <p>Warning</p> <p>When the lock expires before the task completes (<code>max_lock_seconds</code> exceeded), another node may acquire the lock and execute concurrently. A warning is logged in this case.</p>"},{"location":"sync/#leader-election","title":"Leader Election","text":"<p>Leader election ensures that only one worker in the cluster is designated as the leader at any given time using a distributed lock.</p> <p>The leader election service is responsible for acquiring and renewing the distributed lock. It runs as an AnyIO Task that can be easily started with the Task Manager. This service operates in the background, automatically renewing the lock to prevent other workers from acquiring it. The lock is released automatically when the task is cancelled or during shutdown.</p> Task Manager (Recommended)AnyIO Task Group (Advanced) <pre><code>from grelmicro.sync import LeaderElection\nfrom grelmicro.task import TaskManager\n\nleader = LeaderElection(\"cluster_group\")\ntask = TaskManager()\ntask.add_task(leader)\n</code></pre> <pre><code>from anyio import create_task_group, sleep_forever\n\nfrom grelmicro.sync.leaderelection import LeaderElection\n\nleader = LeaderElection(\"cluster_group\")\n\n\nasync def main():\n    async with create_task_group() as tg:\n        await tg.start(leader)\n        await sleep_forever()\n</code></pre>"},{"location":"sync/#lock","title":"Lock","text":"<p>The lock is a distributed lock that can be used to synchronize access to shared resources.</p> <p>The lock supports the following features:</p> <ul> <li>Async: The lock must be acquired and released asynchronously.</li> <li>Distributed: The lock must be distributed across multiple workers.</li> <li>Non-reentrant: Nested acquire from the same task or thread raises <code>LockReentrantError</code>. Use separate instances if you need independent locks.</li> <li>Idempotent backend: The backend allows the same token to re-acquire the lock, extending the lease. Use <code>do_acquire</code> directly if you need to extend the lease explicitly.</li> <li>Expiring: The lock must have a timeout to auto-release after an interval to prevent deadlocks.</li> <li>Non-blocking: Lock operations must not block the async event loop.</li> <li>Vendor-agnostic: Must support multiple backends (Redis, Postgres, ConfigMap, etc.).</li> </ul> <pre><code>from grelmicro.sync import Lock\n\nlock = Lock(\"resource_name\")\n\n\nasync def main():\n    async with lock:\n        print(\"Protected resource accessed\")\n</code></pre> <p>Warning</p> <p>The lock is designed for use within an async event loop and is not thread-safe or process-safe.</p> <p>Want to understand how worker identity and lock tokens work internally?</p> <p>See Synchronization Internals for details on UUID generation, token scoping, and design guarantees.</p>"},{"location":"task/","title":"Task Scheduler","text":"<p>The <code>task</code> package provides a simple task scheduler that can be used to run tasks periodically.</p> <p>Note</p> <p>This is not a replacement for bigger tools like Celery, taskiq, or APScheduler. It is just lightweight, easy to use, and safe for running tasks in a distributed system with synchronization.</p> <p>The key features are:</p> <ul> <li>Fast &amp; Easy: Simple decorators to define and schedule tasks effortlessly.</li> <li>Interval Task: Run tasks at specified intervals, locally or distributed.</li> <li>Synchronization: Control concurrency using distributed primitives (see Synchronization Primitives).</li> <li>Dependency Injection: Use FastDepends to inject dependencies into tasks.</li> <li>Error Handling: Catches and logs errors so that task failures do not stop the scheduler.</li> </ul>"},{"location":"task/#task-manager","title":"Task Manager","text":"<p>The <code>TaskManager</code> class is the main entry point to manage tasks. Start it using the application lifespan:</p> FastAPIFastStream <pre><code>from contextlib import asynccontextmanager\n\nfrom fastapi import FastAPI\n\nfrom grelmicro.task import TaskManager\n\ntask = TaskManager()\n\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    async with task:\n        yield\n\n\napp = FastAPI(lifespan=lifespan)\n</code></pre> <pre><code>from contextlib import asynccontextmanager\n\nfrom faststream import ContextRepo, FastStream\nfrom faststream.redis import RedisBroker\n\nfrom grelmicro.task import TaskManager\n\ntask = TaskManager()\n\n\n@asynccontextmanager\nasync def lifespan(context: ContextRepo):\n    async with task:\n        yield\n\n\nbroker = RedisBroker()\napp = FastStream(broker, lifespan=lifespan)\n</code></pre>"},{"location":"task/#interval-task","title":"Interval Task","text":"<p>Use the <code>interval</code> decorator to run a task at a fixed interval:</p> <p>Note</p> <p>The interval specifies the waiting time between task executions. Ensure that the task execution duration is considered to meet deadlines effectively.</p> TaskManagerTaskRouter <pre><code>from grelmicro.task import TaskManager\n\ntask = TaskManager()\n\n\n@task.interval(seconds=5)\nasync def my_task():\n    print(\"Hello, World!\")\n</code></pre> <pre><code>from grelmicro.task import TaskRouter\n\ntask = TaskRouter()\n\n\n@task.interval(seconds=5)\nasync def my_task():\n    print(\"Hello, World!\")\n</code></pre>"},{"location":"task/#distributed-lock","title":"Distributed Lock","text":"<p>Set <code>max_lock_seconds</code> to enable distributed locking: the task runs at most once per interval across all workers. This uses a built-in <code>TaskLock</code> automatically.</p> <pre><code>from grelmicro.task import TaskManager\n\ntask = TaskManager()\n\n\n@task.interval(seconds=60, max_lock_seconds=300)\nasync def cleanup():\n    print(\"Running cleanup...\")\n</code></pre> Parameter Description <code>seconds</code> Duration in seconds between each scheduling attempt. Each worker retries every N seconds, but only one executes per interval. <code>max_lock_seconds</code> Crash protection TTL. Must be &gt;= <code>seconds</code>. If a worker crashes, the lock expires after this duration. <code>min_lock_seconds</code> Minimum duration to hold the lock after task completion. Prevents re-execution on other nodes too soon. Defaults to <code>seconds</code>."},{"location":"task/#leader-gating","title":"Leader Gating","text":"<p>Gate the task behind a Leader Election so only the leader worker executes it. Setting <code>leader</code> implies distributed locking (with <code>max_lock_seconds</code> defaulting to <code>seconds * 5</code>):</p> <pre><code>from grelmicro.sync import LeaderElection\nfrom grelmicro.task import TaskManager\n\nleader = LeaderElection(\"my-service\")\ntask = TaskManager()\ntask.add_task(leader)\n\n\n@task.interval(seconds=60, leader=leader)\nasync def cleanup():\n    print(\"Running cleanup...\")\n</code></pre>"},{"location":"task/#custom-lock-timing","title":"Custom Lock Timing","text":"<p>For long-running tasks, customize both <code>max_lock_seconds</code> and <code>min_lock_seconds</code>:</p> <pre><code>from grelmicro.task import TaskManager\n\ntask = TaskManager()\n\n\n@task.interval(seconds=60, max_lock_seconds=300, min_lock_seconds=30)\nasync def long_task():\n    print(\"Running long task...\")\n</code></pre>"},{"location":"task/#resource-lock","title":"Resource Lock","text":"<p>Combine distributed locking with a <code>Lock</code> to synchronize access to a shared resource during task execution. Pass the <code>Lock</code> via the <code>sync</code> parameter:</p> <pre><code>from grelmicro.sync import Lock\nfrom grelmicro.task import TaskManager\n\ntask = TaskManager()\nresource_lock = Lock(\"shared-resource\")\n\n\n@task.interval(seconds=60, max_lock_seconds=300, sync=resource_lock)\nasync def cleanup():\n    print(\"Running cleanup...\")\n</code></pre>"},{"location":"task/#how-it-works","title":"How It Works","text":"<p>When the lock is already held, the task skips the execution (logged at DEBUG level) and retries on the next interval.</p> <pre><code>Node A:  [acquire] \u2192 [execute] \u2192 [hold for seconds] \u2192 [TTL expires]\nNode B:  [skip] \u2192 ... \u2192 [skip] \u2192 ... \u2192 [acquire] \u2192 [execute]\n</code></pre> <p>When combining leader gating, distributed locking, and a resource lock, the synchronization primitives are acquired in this order:</p> Order Primitive Purpose 1 <code>LeaderElection</code> Instantly rejects non-leader workers without touching any lock, avoiding unnecessary contention. 2 <code>TaskLock</code> Guarantees at-most-once execution per interval. Acquired after leadership is confirmed to keep the TTL window tight. 3 <code>Lock</code> User-provided lock for shared-resource access. Acquired last so the resource is held only during actual execution. <p>Each primitive is only acquired if the previous one succeeded. For example, a non-leader worker is rejected at step 1 and never touches the task lock or resource lock.</p>"},{"location":"task/#task-router","title":"Task Router","text":"<p>For bigger applications, use the <code>TaskRouter</code> class to organize tasks across modules:</p> <pre><code>\n</code></pre> <p>Then include the <code>TaskRouter</code> into the <code>TaskManager</code> or other routers:</p> <pre><code>from grelmicro.task.manager import TaskManager\n\ntask = TaskManager()\ntask.include_router(router)\n</code></pre> <p>Tip</p> <p>The <code>TaskRouter</code> follows the same philosophy as the <code>APIRouter</code> in FastAPI or the Router in FastStream.</p>"},{"location":"task/#deprecated-apis","title":"Deprecated APIs","text":""},{"location":"task/#scheduled-task","title":"Scheduled Task","text":"<p>Deprecated</p> <p>The <code>scheduled()</code> decorator is deprecated. Use <code>interval()</code> with <code>max_lock_seconds</code> or <code>leader</code> instead.</p> <p>The <code>scheduled()</code> decorator still works but emits a <code>DeprecationWarning</code>. It is equivalent to <code>interval(seconds=N, max_lock_seconds=N*5, min_lock_seconds=N)</code>.</p>"},{"location":"task/#sync-with-tasklock-or-leaderelection","title":"sync with TaskLock or LeaderElection","text":"<p>Deprecated</p> <p>Using <code>sync</code> with <code>TaskLock</code> or <code>LeaderElection</code> is deprecated. Use <code>max_lock_seconds</code> and <code>leader</code> parameters instead.</p> <p>See Synchronization Primitives for more details.</p>"},{"location":"tracing/","title":"Tracing","text":"<p>The <code>tracing</code> module provides unified instrumentation inspired by Rust's tracing crate. A single <code>@instrument</code> decorator creates OTel spans and enriches log records with structured context automatically.</p>"},{"location":"tracing/#quick-start","title":"Quick Start","text":"<pre><code>from grelmicro.logging import configure_logging\nfrom grelmicro.tracing import instrument, span, add_context\nimport logging\n\nconfigure_logging()\nlogger = logging.getLogger(__name__)\n\n@instrument\nasync def process_order(order_id: str, user_id: str):\n    logger.info(\"started\")\n    # {\"time\":...,\"level\":\"INFO\",\"msg\":\"started\",\"logger\":...,\"order_id\":\"ORD-1\",\"user_id\":\"USR-1\"}\n\n    add_context(payment_status=\"pending\")\n    logger.info(\"payment initiated\")\n    # {\"time\":...,\"level\":\"INFO\",\"msg\":\"payment initiated\",\"logger\":...,\"order_id\":\"ORD-1\",\"user_id\":\"USR-1\",\"payment_status\":\"pending\"}\n\n    with span(\"db_query\", table=\"orders\"):\n        logger.info(\"querying\")\n        # {\"time\":...,\"level\":\"INFO\",\"msg\":\"querying\",\"logger\":...,\"order_id\":\"ORD-1\",\"user_id\":\"USR-1\",\"payment_status\":\"pending\",\"table\":\"orders\"}\n\n    logger.info(\"done\")\n    # table removed (span exited), payment_status still present\n</code></pre>"},{"location":"tracing/#api","title":"API","text":""},{"location":"tracing/#instrument","title":"<code>@instrument</code>","text":"<p>Decorator that captures function arguments as structured context. Works with sync and async functions.</p> <pre><code># Bare decorator: captures all arguments\n@instrument\nasync def process(order_id: str, user_id: str): ...\n\n# Skip sensitive arguments\n@instrument(skip={\"password\", \"token\"})\nasync def login(username: str, password: str): ...\n\n# Skip all arguments\n@instrument(skip_all=True)\nasync def bulk_process(payload: bytes): ...\n\n# Custom span name\n@instrument(name=\"db.query\")\nasync def fetch_user(user_id: str): ...\n</code></pre>"},{"location":"tracing/#span","title":"<code>span()</code>","text":"<p>Context manager for mid-function instrumentation. Creates a nested context that is automatically removed on exit.</p> <pre><code>@instrument\nasync def handle_request(request_id: str):\n    logger.info(\"received\")  # has request_id\n\n    with span(\"auth\", method=\"jwt\"):\n        logger.info(\"authenticating\")  # has request_id + method\n\n    with span(\"db\", table=\"users\"):\n        logger.info(\"querying\")  # has request_id + table\n\n    logger.info(\"done\")  # back to request_id only\n</code></pre>"},{"location":"tracing/#add_context","title":"<code>add_context()</code>","text":"<p>Add fields to the current context. Updates both log records and the active OTel span (if tracing is configured).</p> <pre><code>@instrument\nasync def process(order_id: str):\n    result = charge()\n    add_context(payment_id=result.id, status=result.status)\n    logger.info(\"charged\")  # includes payment_id and status\n</code></pre>"},{"location":"tracing/#configuration","title":"Configuration","text":"<p>The tracing context enriches log records regardless of how logging is configured. No additional configuration is needed.</p> <p>When OpenTelemetry is installed, <code>@instrument</code> and <code>span()</code> also create OTel spans with the same fields as attributes. This means one decorator gives you both structured logs and distributed traces.</p> <pre><code># Logging only (no OTel dependency needed)\nconfigure_logging()\n\n# Logging + OTel: install opentelemetry and configure your exporter separately.\n# @instrument and span() will automatically create OTel spans when opentelemetry\n# is installed and a TracerProvider is configured.\n</code></pre>"},{"location":"tracing/#works-with-all-backends","title":"Works With All Backends","text":"<p>The tracing context is injected into all three logging backends (stdlib, loguru, structlog). Use whichever logger you prefer:</p> <pre><code># stdlib\nimport logging\nlogger = logging.getLogger(__name__)\nlogger.info(\"message\", extra={\"key\": \"value\"})\n\n# loguru\nfrom loguru import logger\nlogger.info(\"message\", key=\"value\")\n\n# structlog\nimport structlog\nlog = structlog.get_logger()\nlog.info(\"message\", key=\"value\")\n</code></pre> <p>All produce the same JSON output with tracing context included.</p>"},{"location":"architecture/","title":"Architecture","text":"<p>This section documents the internal design decisions and guarantees of grelmicro.</p> <ul> <li>Backend Registry: Shared registry pattern for swappable backends.</li> <li>Import Strategy: Why backends are imported from submodules, not re-exported.</li> <li>Synchronization: Worker identity, token generation, lock design, and cleanup strategy.</li> <li>Kubernetes Backend: Lease resources, optimistic concurrency, and name sanitization.</li> <li>SQLite Backend: WAL mode.</li> <li>Tracing: Context stack, concurrency safety, and decoupled layering.</li> </ul>"},{"location":"architecture/backends/","title":"Backend Registry","text":"<p>grelmicro uses a shared backend registry pattern to make infrastructure backends (Redis, PostgreSQL, SQLite, etc.) swappable without changing application code.</p>"},{"location":"architecture/backends/#why-async","title":"Why Async","text":"<p>All backends use async methods because they perform network or disk I/O (Redis, PostgreSQL, SQLite, Kubernetes API). Async avoids blocking the event loop, which is critical in microservice applications handling many concurrent requests. Even backends with low-latency I/O (like SQLite) use async to maintain a consistent interface and allow the event loop to schedule other work during I/O waits.</p>"},{"location":"architecture/backends/#design","title":"Design","text":"<p>The <code>BackendRegistry[T]</code> is a generic, typed container that holds a single default backend instance. Each module maintains its own registry:</p> Module Registry Protocol Backends <code>sync</code> <code>sync_backend_registry</code> <code>SyncBackend</code> Redis, PostgreSQL, SQLite, Kubernetes, Memory <code>cache</code> <code>cache_backend_registry</code> <code>CacheBackend</code> Redis, Memory"},{"location":"architecture/backends/#registration","title":"Registration","text":"<p>Backends register themselves on initialization via <code>auto_register=True</code> (the default):</p> <pre><code>async with RedisSyncBackend() as backend:\n    # backend is now the default for Lock, TaskLock, LeaderElection\n    ...\n</code></pre> <p>This calls <code>sync_backend_registry.set(self)</code> internally. Consumers that accept an optional <code>backend</code> parameter fall back to the registry when none is provided:</p> <pre><code># Explicit backend\nlock = Lock(name=\"my-lock\", backend=my_backend)\n\n# Uses the registered default\nlock = Lock(name=\"my-lock\")\n</code></pre>"},{"location":"architecture/backends/#protocol-based-polymorphism","title":"Protocol-Based Polymorphism","text":"<p>Backends are defined by protocols (structural typing), not base classes. Any object implementing the required methods works as a backend. This enables:</p> <ul> <li>Swapping backends without changing application code</li> <li>Writing test backends (e.g. <code>MemorySyncBackend</code>) with no external dependencies</li> <li>Adding new backends without modifying existing code</li> </ul>"},{"location":"architecture/backends/#auto-registration-control","title":"Auto-Registration Control","text":"<p>Set <code>auto_register=False</code> to create a backend without registering it as the default. This is useful for:</p> <ul> <li>Tests that need isolated backend instances</li> <li>Applications using multiple backends for different purposes</li> <li>Manual wiring in dependency injection setups</li> </ul> <pre><code># Not registered as default\ncache = RedisCache(url=\"redis://localhost/1\", ttl=60, auto_register=False)\n</code></pre>"},{"location":"architecture/backends/#connection-pool-isolation","title":"Connection Pool Isolation","text":"<p>Each backend instance creates its own Redis client and connection pool, even when multiple backends point to the same Redis server. This is an intentional design choice:</p> <ul> <li>Failure isolation: a slow lock Lua script cannot starve cache reads (and vice-versa)</li> <li>Independent lifecycle: each backend opens and closes on its own schedule via <code>async with</code></li> <li>Independent tuning: pool settings (<code>max_connections</code>, timeouts) can be configured per domain</li> <li>No hidden coupling: closing the cache backend does not affect sync locks</li> </ul> <p>The overhead is negligible (a few extra TCP connections, created lazily) and this approach follows the Python ecosystem standard (Django, SQLAlchemy, and Celery all use separate connection pools per concern).</p> <p>Shared Redis configuration (URL resolution from environment variables) is deduplicated in <code>grelmicro/_redis.py</code>, but each backend receives its own client instance.</p>"},{"location":"architecture/backends/#error-handling","title":"Error Handling","text":"<p>Accessing a registry before any backend is registered raises <code>BackendNotLoadedError</code> with a descriptive message:</p> <pre><code>No lock backend loaded. Initialize a backend first\n(e.g. with ``async with`` a backend instance).\n</code></pre>"},{"location":"architecture/imports/","title":"Import Strategy","text":"<p>grelmicro follows the Django/SQLAlchemy pattern: core API is re-exported from package <code>__init__.py</code>, backends are imported from their submodules.</p>"},{"location":"architecture/imports/#principle","title":"Principle","text":"Import from Contains Package (<code>grelmicro.sync</code>) Primitives, protocols, errors, types (what every user needs) Submodule (<code>grelmicro.sync.redis</code>) Backend implementations (infrastructure-specific code)"},{"location":"architecture/imports/#rationale","title":"Rationale","text":"<p>No unnecessary imports. Importing a primitive does not pull in backend client libraries. Users only pay for the backend they choose.</p> <p>Explicit dependencies. A submodule import like <code>from grelmicro.sync.redis import RedisSyncBackend</code> makes the infrastructure dependency visible at the import site. Grepping for the submodule path finds every file that needs that backend.</p> <p>Ecosystem convention. Django databases, SQLAlchemy dialects, and Celery brokers all follow this pattern. Backends are selected once in configuration, not scattered across business logic.</p>"},{"location":"architecture/kubernetes/","title":"Kubernetes Backend","text":"<p>This page documents the internal design of the Kubernetes Synchronization Backend.</p>"},{"location":"architecture/kubernetes/#lease-resources","title":"Lease Resources","text":"<p>The backend uses Lease resources from the <code>coordination.k8s.io/v1</code> API group. Leases are the idiomatic Kubernetes resource for coordination: they are used by kube-scheduler and client-go leader election. Unlike ConfigMaps, Leases have a dedicated schema for holder identity, duration, and timestamps.</p>"},{"location":"architecture/kubernetes/#field-mapping","title":"Field Mapping","text":"Lease Field Lock Concept <code>holderIdentity</code> Lock token <code>leaseDurationSeconds</code> <code>ceil(duration)</code> <code>acquireTime</code> / <code>renewTime</code> Set on acquire/extend"},{"location":"architecture/kubernetes/#optimistic-concurrency","title":"Optimistic Concurrency","text":"<p>The backend uses Kubernetes resourceVersion for optimistic concurrency control:</p> <ul> <li>Acquire: GET the Lease. If 404 \u2192 CREATE. If expired or same token \u2192 REPLACE (with resourceVersion). If held by another \u2192 return <code>False</code>. On 409 Conflict \u2192 return <code>False</code>.</li> <li>Release: GET the Lease. If token matches and not expired \u2192 DELETE. Otherwise <code>False</code>.</li> <li>Locked: GET the Lease. Check <code>renewTime + leaseDurationSeconds &gt;= now</code>.</li> <li>Owned: GET the Lease. Check <code>holderIdentity == token</code> and <code>renewTime + leaseDurationSeconds &gt;= now</code>.</li> </ul> <p>The REPLACE operation includes the <code>resourceVersion</code> from the GET, so the API server rejects the update with a 409 Conflict if another client modified the Lease in between. This provides the same atomicity guarantees as database transactions.</p>"},{"location":"architecture/kubernetes/#lease-labeling","title":"Lease Labeling","text":"<p>All Lease resources created by grelmicro are labeled with <code>app.kubernetes.io/managed-by: grelmicro</code>. This label is used during cleanup (<code>__aexit__</code>) to filter only grelmicro-managed leases, preventing accidental deletion of leases owned by kube-scheduler or other controllers.</p>"},{"location":"architecture/kubernetes/#name-sanitization","title":"Name Sanitization","text":"<p>Lock names (e.g., <code>\"lock:my-resource\"</code>) must become valid Kubernetes resource names per RFC 1123:</p> <ul> <li>Lowercase alphanumeric characters and hyphens only</li> <li>Must start and end with an alphanumeric character</li> <li>Maximum 253 characters</li> </ul> <p>The sanitization strategy replaces invalid characters with hyphens, collapses consecutive hyphens, truncates to 253 characters, and strips leading/trailing hyphens. The resulting lease names are human-readable and visible via <code>kubectl get lease</code>.</p>"},{"location":"architecture/kubernetes/#multi-app-isolation","title":"Multi-App Isolation","text":"<p>When multiple applications share the same Kubernetes namespace, use the <code>prefix</code> parameter to avoid lease name collisions (similar to Redis's <code>prefix</code> parameter). For example:</p> <pre><code>backend = KubernetesSyncBackend(namespace=\"default\", prefix=\"myapp-\")\n</code></pre> <p>This prepends <code>myapp-</code> to every lease name before sanitization, ensuring different applications cannot interfere with each other's locks.</p>"},{"location":"architecture/kubernetes/#client-library","title":"Client Library","text":"<p>The backend uses lightkube as the Kubernetes async client. lightkube is lightweight, async-native (built on httpx), and provides strong typing for Kubernetes resources.</p>"},{"location":"architecture/sqlite/","title":"SQLite Backend","text":"<p>This page documents the internal design of the SQLite Synchronization Backend.</p>"},{"location":"architecture/sqlite/#wal-mode","title":"WAL Mode","text":"<p>The backend enables Write-Ahead Logging (WAL) on connection with <code>PRAGMA journal_mode=WAL</code>. Without WAL, SQLite uses a rollback journal where writers block readers and readers block writers. WAL allows concurrent reads and writes, which is important for async lock operations where multiple tasks may check or acquire locks simultaneously.</p> <p>WAL mode is persistent per database file. Once enabled, it remains active for all subsequent connections until explicitly changed.</p>"},{"location":"architecture/sync/","title":"Synchronization Internals","text":"<p>This page documents the internal design of the Synchronization Primitives.</p>"},{"location":"architecture/sync/#worker-identity","title":"Worker Identity","text":"<p>By default, each synchronization primitive (<code>Lock</code>, <code>TaskLock</code>, <code>LeaderElection</code>) generates a unique worker identity at instantiation using <code>token_hex(4)</code> (8 random hex characters, 32 bits of entropy) when no explicit <code>worker</code> parameter is provided.</p> <p>This provides uniqueness across:</p> <ul> <li>Multiple processes (e.g., <code>uvicorn --workers N</code>): Each worker process imports the application independently, so <code>token_hex(4)</code> is called separately per process with independent randomness.</li> <li>Multiple instances within the same process: Each <code>Lock(...)</code> or <code>TaskLock(...)</code> call generates its own <code>token_hex(4)</code>, producing a different worker identity.</li> </ul> <p>Pre-fork servers</p> <p>If the ASGI server uses a pre-fork model (forking after the application is loaded), worker identities generated before the fork will be duplicated across child processes. Uvicorn does not pre-fork. It spawns workers via <code>subprocess.Popen</code>, so each worker imports the application independently. If using a pre-fork server, pass an explicit <code>worker</code> identity to avoid collisions.</p>"},{"location":"architecture/sync/#token-generation","title":"Token Generation","text":"<p>Lock tokens identify who holds a lock. They are derived deterministically from the worker identity and the current execution context using simple string concatenation:</p> Primitive Token Scope <code>Lock</code> <code>{worker}:task:{task_id}</code> Per async task <code>Lock.from_thread</code> <code>{worker}:thread:{thread_id}</code> Per thread <code>TaskLock</code> <code>{worker}:task:{task_id}</code> Per async task <code>TaskLock.from_thread</code> <code>{worker}:thread:{thread_id}</code> Per thread <code>LeaderElection</code> <code>worker</code> directly Per process <p>This design provides the following guarantees:</p> <ul> <li>Mutual exclusion: Different async tasks or threads produce different tokens for the same lock, ensuring only one caller holds the lock at a time.</li> <li>Idempotent: The same async task (or thread) always produces the same deterministic token, so the backend accepts a re-acquire and extends the lease.</li> <li>Isolation: Different lock instances have different worker identities, so their tokens never collide even when used from the same task or thread.</li> </ul>"},{"location":"architecture/sync/#lock-name-and-backend-key","title":"Lock Name and Backend Key","text":"<p>Each synchronization primitive automatically prefixes the user-provided <code>name</code> with a type-specific namespace to form the backend key:</p> Primitive Name Backend Key <code>Lock(\"my-resource\")</code> <code>my-resource</code> <code>lock:my-resource</code> <code>TaskLock(\"cleanup\")</code> <code>cleanup</code> <code>tasklock:cleanup</code> <code>LeaderElection(\"main\")</code> <code>main</code> <code>leader:main</code> <p>This prevents accidental collisions between different primitive types sharing the same backend. A <code>Lock(\"x\")</code> and a <code>TaskLock(\"x\")</code> operate on independent backend entries.</p> <p>Breaking change</p> <p>Prior versions used the <code>name</code> parameter directly as the backend key without any prefix. After upgrading, existing locks stored in backends (Redis, PostgreSQL) will no longer match. Ensure all running instances are upgraded together so they use the same key format.</p>"},{"location":"architecture/sync/#lock-cleanup","title":"Lock Cleanup","text":"<p>Expired locks are never actively removed during normal operation. Instead, all backends use a lazy filtering strategy combined with cleanup on exit:</p> <ol> <li>Lazy filtering: Every <code>locked()</code>, <code>owned()</code>, and <code>acquire()</code> call includes an expiry check (<code>expire_at &gt;= now</code>), so expired locks are simply ignored without requiring deletion.</li> <li>Cleanup on exit: When the backend context manager exits (<code>__aexit__</code>), all expired locks are deleted in bulk. This keeps storage clean across graceful restarts.</li> </ol> <p>If the process crashes without exiting the context manager, expired locks remain in storage but are harmless: they will be filtered out by all subsequent operations and cleaned up on the next graceful shutdown.</p>"},{"location":"architecture/sync/#backend-specific-cleanup","title":"Backend-specific cleanup","text":"<ul> <li>SQLite / PostgreSQL: A single bulk <code>DELETE ... WHERE expire_at &lt; now</code> removes all stale rows before closing the connection.</li> <li>Kubernetes: Lists all Lease resources labeled <code>app.kubernetes.io/managed-by: grelmicro</code> and deletes each expired lease individually. The Kubernetes API does not support bulk conditional deletion. <code>NOT_FOUND</code> errors are silently ignored to handle concurrent deletions.</li> </ul>"},{"location":"architecture/tracing/","title":"Tracing Architecture","text":""},{"location":"architecture/tracing/#context-stack","title":"Context Stack","text":"<p>The tracing module uses a <code>contextvars</code>-based context stack defined in <code>grelmicro/_context.py</code>. This low-level module is shared by both <code>logging</code> and <code>tracing</code> so that neither depends on the other.</p> <p>Each <code>@instrument</code> call or <code>span()</code> block pushes a new frame with its fields. Logging backends merge all active frames into the JSON record.</p> <pre><code>@instrument(order_id=\"ORD-1\")     &lt;- frame 1: {order_id: \"ORD-1\"}\n  add_context(status=\"pending\")   &lt;- frame 1: {order_id: \"ORD-1\", status: \"pending\"}\n  with span(\"db\", table=\"users\")  &lt;- frame 2: {table: \"users\"}\n    logger.info(\"query\")          &lt;- merged: {order_id, status, table}\n  # frame 2 popped               &lt;- merged: {order_id, status}\n</code></pre>"},{"location":"architecture/tracing/#concurrency-safety","title":"Concurrency Safety","text":"<p>The stack is stored as a <code>ContextVar[tuple[dict[str, Any], ...]]</code>. Both the tuple and the dicts are replaced (never mutated) to ensure concurrent async tasks sharing a parent context are isolated:</p> <ul> <li>Each <code>asyncio.Task</code> gets a copy of the current context when created.</li> <li><code>asyncio.to_thread()</code> also copies the context into the thread.</li> <li><code>add_context()</code> replaces the top frame (not mutates it), so sibling tasks cannot interfere.</li> </ul> <p>This follows the same pattern used by OpenTelemetry Python for <code>Context</code> propagation.</p>"},{"location":"architecture/tracing/#decoupled-layering","title":"Decoupled Layering","text":"<pre><code>grelmicro/_context.py    &lt;- owns ContextVar (no dependencies)\n    \u251c\u2500\u2500 logging/         &lt;- imports _context (merge into log records)\n    \u2514\u2500\u2500 tracing/         &lt;- imports _context (push/pop spans, add_context)\n</code></pre> <p>The <code>_context</code> module has no imports from <code>logging</code> or <code>tracing</code>, preventing circular dependencies and allowing users to configure logging without loading the tracing module.</p>"},{"location":"reference/cache/","title":"Cache","text":"<p>Cache.</p> <p>Memory Cache Backend.</p> <p>Redis Cache Backend.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheBackend","title":"CacheBackend","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for cache storage backends.</p> <p>All methods are async because backends typically perform I/O. Backends are pure key-value stores: TTL, eviction, and statistics are managed by <code>TTLCache</code>.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheBackend.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Open the backend connection.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheBackend.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; None\n</code></pre> <p>Close the backend connection.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheBackend.get","title":"get  <code>async</code>","text":"<pre><code>get(*, key: str) -&gt; bytes | None\n</code></pre> <p>Get raw bytes by key.</p> <p>Returns None if the key is missing or expired.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheBackend.set","title":"set  <code>async</code>","text":"<pre><code>set(*, key: str, value: bytes, ttl: float) -&gt; None\n</code></pre> <p>Store raw bytes with a TTL in seconds.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheBackend.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(*, key: str) -&gt; None\n</code></pre> <p>Delete a key (no-op if absent).</p>"},{"location":"reference/cache/#grelmicro.cache.CacheBackend.clear","title":"clear  <code>async</code>","text":"<pre><code>clear() -&gt; None\n</code></pre> <p>Remove all entries managed by this backend.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheError","title":"CacheError","text":"<p>               Bases: <code>GrelmicroError</code></p> <p>Base cache error.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheInfo","title":"CacheInfo  <code>dataclass</code>","text":"<pre><code>CacheInfo(\n    hits: int,\n    misses: int,\n    maxsize: int,\n    currsize: int,\n    evictions: int,\n)\n</code></pre> <p>Cache statistics snapshot.</p> <p>Attributes:</p> Name Type Description <code>hits</code> <code>int</code> <p>Number of cache hits.</p> <code>misses</code> <code>int</code> <p>Number of cache misses.</p> <code>maxsize</code> <code>int</code> <p>Maximum number of entries (<code>0</code> means unlimited).</p> <code>currsize</code> <code>int</code> <p>Current number of tracked entries.</p> <code>evictions</code> <code>int</code> <p>Number of entries evicted to make room.</p>"},{"location":"reference/cache/#grelmicro.cache.CacheSettingsValidationError","title":"CacheSettingsValidationError","text":"<pre><code>CacheSettingsValidationError(error: ValidationError | str)\n</code></pre> <p>               Bases: <code>CacheError</code>, <code>SettingsValidationError</code></p> <p>Cache Settings Validation Error.</p>"},{"location":"reference/cache/#grelmicro.cache.TTLCache","title":"TTLCache","text":"<pre><code>TTLCache(\n    maxsize: Annotated[\n        int,\n        Doc(\n            \"\\n                Maximum number of entries. ``0`` means unlimited.\\n                Only enforced locally (not by the backend).\\n                \"\n        ),\n    ] = 0,\n    ttl: Annotated[\n        float,\n        Doc(\n            \"\\n                Default TTL in seconds for all entries.\\n                \"\n        ),\n    ] = 60,\n    *,\n    backend: Annotated[\n        CacheBackend | None,\n        Doc(\n            \"\\n                The cache storage backend.\\n\\n                By default, the registered cache backend is used.\\n                \"\n        ),\n    ] = None,\n    serializer: Annotated[\n        Callable[[T], bytes] | None,\n        Doc(\n            \"\\n                Optional function to serialize values to bytes\\n                before storing in the backend.\\n                \"\n        ),\n    ] = None,\n    deserializer: Annotated[\n        Callable[[bytes], T] | None,\n        Doc(\n            \"\\n                Optional function to deserialize bytes from the\\n                backend back into values.\\n                \"\n        ),\n    ] = None,\n)\n</code></pre> <p>               Bases: <code>Generic[T]</code></p> <p>Cache with per-entry TTL and optional LRU eviction.</p> <p>Delegates storage to a <code>CacheBackend</code> (in-memory, Redis, etc.). TTLCache handles maxsize enforcement, LRU eviction, serialization, and statistics on top of the backend.</p> <p>When no backend is provided, the registered default is used (see <code>MemoryCacheBackend</code> or <code>RedisCacheBackend</code>).</p> <p>The type parameter <code>T</code> represents the cached value type. Defaults to <code>Any</code> when unspecified (<code>TTLCache()</code>). Use <code>TTLCache[bytes]</code> for raw bytes or <code>TTLCache[MyModel]</code> with a serializer/deserializer pair for typed caching.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If maxsize is negative or ttl is not positive.</p> <p>Initialize the cache.</p>"},{"location":"reference/cache/#grelmicro.cache.TTLCache.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    key: Annotated[str, Doc(\"The cache key.\")],\n    default: Annotated[\n        T | None,\n        Doc(\n            \"Value to return if the key is missing or expired.\"\n        ),\n    ] = None,\n) -&gt; T | None\n</code></pre> <p>Get a value by key.</p> <p>Returns the default if the key is missing or expired. A hit promotes the key in LRU order.</p>"},{"location":"reference/cache/#grelmicro.cache.TTLCache.set","title":"set  <code>async</code>","text":"<pre><code>set(\n    key: Annotated[str, Doc(\"The cache key.\")],\n    value: Annotated[\n        T,\n        Doc(\n            \"The value to store. Must be bytes or serializable.\"\n        ),\n    ],\n    ttl: Annotated[\n        float | None,\n        Doc(\n            \"Per-entry TTL override in seconds. Uses the default TTL if None.\"\n        ),\n    ] = None,\n) -&gt; None\n</code></pre> <p>Set a value with an optional per-entry TTL override.</p> <p>If the cache is full (maxsize &gt; 0), evicts the least recently used entry before storing.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If ttl is not positive.</p> <code>TypeError</code> <p>If value is not bytes and no serializer is set.</p>"},{"location":"reference/cache/#grelmicro.cache.TTLCache.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    key: Annotated[str, Doc(\"The cache key to delete.\")],\n) -&gt; None\n</code></pre> <p>Delete a key from the cache.</p> <p>No-op if the key does not exist.</p>"},{"location":"reference/cache/#grelmicro.cache.TTLCache.clear","title":"clear  <code>async</code>","text":"<pre><code>clear() -&gt; None\n</code></pre> <p>Remove all entries from the cache.</p>"},{"location":"reference/cache/#grelmicro.cache.TTLCache.cache_info","title":"cache_info","text":"<pre><code>cache_info() -&gt; CacheInfo\n</code></pre> <p>Return a snapshot of cache statistics.</p>"},{"location":"reference/cache/#grelmicro.cache.cached","title":"cached","text":"<p>Cached Decorator.</p>"},{"location":"reference/cache/#grelmicro.cache.cached.cached","title":"cached","text":"<pre><code>cached(\n    cache: Annotated[\n        TTLCache,\n        Doc(\n            \"\\n            The TTLCache instance to store results in.\\n            \"\n        ),\n    ],\n    *,\n    key_maker: Annotated[\n        Callable[\n            [\n                Callable[..., Any],\n                tuple[Any, ...],\n                dict[str, Any],\n            ],\n            str,\n        ]\n        | None,\n        Doc(\n            \"\\n            Optional custom key generation function. Receives\\n            ``(func, args, kwargs)`` and must return a string key.\\n            \"\n        ),\n    ] = None,\n    skip: Annotated[\n        Callable[[Any], bool] | None,\n        Doc(\n            \"\\n            Optional predicate receiving the function result. When\\n            it returns ``True`` the result is **not** cached.\\n            \"\n        ),\n    ] = None,\n    typed: Annotated[\n        bool,\n        Doc(\n            \"\\n            If ``True``, arguments of different types are cached\\n            separately (e.g. ``3`` vs ``3.0``).\\n            \"\n        ),\n    ] = False,\n    lock: Annotated[\n        _LockType,\n        Doc(\n            \"\\n            Enable stampede protection. When a cache miss occurs,\\n            only one caller executes the function while all others\\n            block until the result is available.\\n\\n            Set to ``True`` to enable with **per-key** locking:\\n            concurrent misses on different keys proceed in parallel,\\n            only callers hitting the same key are serialized.\\n            The appropriate lock type is auto-created\\n            (``asyncio.Lock`` for async, ``threading.Lock`` for\\n            sync).\\n\\n            You can also pass a custom context manager instance\\n            for **global** locking (a single lock shared across\\n            all keys).\\n            \"\n        ),\n    ] = None,\n) -&gt; Callable[[Callable[P, R]], Callable[P, R]]\n</code></pre> <p>Cache decorator for sync and async functions.</p> <p>Automatically detects whether the decorated function is sync or async and wraps it accordingly.</p> <p>The decorated function exposes <code>cache_info()</code> and <code>cache_clear()</code> helpers (matching <code>functools.lru_cache</code>). <code>cache_clear()</code> is always a coroutine (must be awaited).</p> <p>Returns:</p> Type Description <code>Callable[[Callable[P, R]], Callable[P, R]]</code> <p>A decorator that caches function results.</p>"},{"location":"reference/cache/#grelmicro.cache.memory.MemoryCacheBackend","title":"MemoryCacheBackend","text":"<pre><code>MemoryCacheBackend(\n    *,\n    auto_register: Annotated[\n        bool,\n        Doc(\n            \"Automatically register this cache backend in the backend registry.\"\n        ),\n    ] = True,\n)\n</code></pre> <p>In-memory cache backend.</p> <p>Stores entries in a Python dict with lazy TTL expiry. Suitable for testing and single-process applications.</p> <p>Initialize the memory cache backend.</p>"},{"location":"reference/cache/#grelmicro.cache.memory.MemoryCacheBackend.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Open the cache backend.</p>"},{"location":"reference/cache/#grelmicro.cache.memory.MemoryCacheBackend.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; None\n</code></pre> <p>Close the cache backend.</p>"},{"location":"reference/cache/#grelmicro.cache.memory.MemoryCacheBackend.get","title":"get  <code>async</code>","text":"<pre><code>get(*, key: str) -&gt; bytes | None\n</code></pre> <p>Get raw bytes by key.</p> <p>Returns None if the key is missing or expired. Expired entries are removed lazily on access.</p>"},{"location":"reference/cache/#grelmicro.cache.memory.MemoryCacheBackend.set","title":"set  <code>async</code>","text":"<pre><code>set(*, key: str, value: bytes, ttl: float) -&gt; None\n</code></pre> <p>Store raw bytes with a TTL in seconds.</p>"},{"location":"reference/cache/#grelmicro.cache.memory.MemoryCacheBackend.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(*, key: str) -&gt; None\n</code></pre> <p>Delete a key (no-op if absent).</p>"},{"location":"reference/cache/#grelmicro.cache.memory.MemoryCacheBackend.clear","title":"clear  <code>async</code>","text":"<pre><code>clear() -&gt; None\n</code></pre> <p>Remove all entries.</p>"},{"location":"reference/cache/#grelmicro.cache.redis.RedisCacheBackend","title":"RedisCacheBackend","text":"<pre><code>RedisCacheBackend(\n    url: Annotated[\n        RedisDsn | str | None,\n        Doc(\n            \"\\n                The Redis URL.\\n\\n                If not provided, the URL will be taken from the\\n                environment variables REDIS_URL or REDIS_HOST,\\n                REDIS_PORT, REDIS_DB, and REDIS_PASSWORD.\\n                \"\n        ),\n    ] = None,\n    *,\n    prefix: Annotated[\n        str,\n        Doc(\n            \"\\n                Prefix prepended to all Redis keys to avoid\\n                conflicts with other keys.\\n\\n                By default no prefix is added.\\n                \"\n        ),\n    ] = \"\",\n    auto_register: Annotated[\n        bool,\n        Doc(\n            \"Automatically register this cache backend in the backend registry.\"\n        ),\n    ] = True,\n)\n</code></pre> <p>Redis cache storage backend.</p> <p>Pure key-value storage with per-entry TTL handled natively by Redis (SETEX). Keys are prefixed for isolation.</p> <p>Must be used as an async context manager to manage the connection lifecycle.</p> <p>Initialize the Redis cache backend.</p>"},{"location":"reference/cache/#grelmicro.cache.redis.RedisCacheBackend.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Open the cache connection.</p>"},{"location":"reference/cache/#grelmicro.cache.redis.RedisCacheBackend.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; None\n</code></pre> <p>Close the cache connection.</p>"},{"location":"reference/cache/#grelmicro.cache.redis.RedisCacheBackend.get","title":"get  <code>async</code>","text":"<pre><code>get(*, key: str) -&gt; bytes | None\n</code></pre> <p>Get raw bytes by key.</p> <p>Returns None if the key is missing or expired.</p>"},{"location":"reference/cache/#grelmicro.cache.redis.RedisCacheBackend.set","title":"set  <code>async</code>","text":"<pre><code>set(*, key: str, value: bytes, ttl: float) -&gt; None\n</code></pre> <p>Store raw bytes with a TTL in seconds.</p>"},{"location":"reference/cache/#grelmicro.cache.redis.RedisCacheBackend.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(*, key: str) -&gt; None\n</code></pre> <p>Delete a key (no-op if absent).</p>"},{"location":"reference/cache/#grelmicro.cache.redis.RedisCacheBackend.clear","title":"clear  <code>async</code>","text":"<pre><code>clear() -&gt; None\n</code></pre> <p>Remove all entries matching the configured prefix.</p> <p>Uses SCAN to iterate keys without blocking Redis, then deletes in batches.</p>"},{"location":"reference/logging/","title":"Logging","text":"<p>Logging.</p> <p>Uvicorn-friendly formatters for dictConfig usage.</p>"},{"location":"reference/logging/#grelmicro.logging.JSONRecordDict","title":"JSONRecordDict","text":"<p>               Bases: <code>TypedDict</code></p> <p>Structured JSON log record (post-serialization schema).</p> <p>Describes the shape of each log line after JSON serialization. Internally, formatters use <code>datetime</code> objects for <code>time</code> and delegate ISO 8601 conversion to the JSON serializer.</p> <p>Core fields follow industry conventions (slog, zap, zerolog). Extra context fields are merged flat at the top level.</p> <p>Example::</p> <pre><code>{\"time\": \"2026-03-30T14:00:00+00:00\", \"level\": \"INFO\",\n \"msg\": \"request handled\", \"logger\": \"myapp.api\",\n \"caller\": \"handle:45\",\n \"method\": \"GET\", \"status\": 200}\n</code></pre>"},{"location":"reference/logging/#grelmicro.logging.configure_logging","title":"configure_logging","text":"<pre><code>configure_logging() -&gt; None\n</code></pre> <p>Configure logging with the selected backend.</p> <p>Simple twelve-factor app logging configuration that logs to stdout.</p> Environment Variables <p>LOG_BACKEND: Logging backend (stdlib, loguru, structlog). Default: stdlib LOG_LEVEL: Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL). Default: INFO LOG_FORMAT: Log format (AUTO, JSON, LOGFMT, TEXT, PRETTY). Default: AUTO LOG_TIMEZONE: IANA timezone for timestamps (e.g., \"UTC\", \"Europe/Zurich\"). Default: UTC LOG_JSON_SERIALIZER: JSON serializer (stdlib, orjson). Default: stdlib LOG_OTEL_ENABLED: Enable OpenTelemetry trace context extraction.     Default: True if OpenTelemetry is installed, else False.</p> <p>Raises:</p> Type Description <code>DependencyNotFoundError</code> <p>If the selected backend module is not installed.</p> <code>LoggingSettingsValidationError</code> <p>If environment variables are invalid.</p>"},{"location":"reference/logging/#grelmicro.logging.uvicorn.UvicornFormatter","title":"UvicornFormatter","text":"<pre><code>UvicornFormatter()\n</code></pre> <p>               Bases: <code>_UvicornBaseFormatter</code></p> <p>Format-aware uvicorn formatter compatible with <code>logging.config.dictConfig</code>.</p> <p>Reads <code>LOG_FORMAT</code> and produces the matching output (AUTO, JSON, LOGFMT, TEXT, PRETTY).  No constructor arguments required.</p> <p>Initialize with settings from environment variables.</p>"},{"location":"reference/logging/#grelmicro.logging.uvicorn.UvicornFormatter.format","title":"format","text":"<pre><code>format(record: LogRecord) -&gt; str\n</code></pre> <p>Format the log record.</p> <p><code>caller</code> is always disabled (<code>caller_enabled=False</code>) because uvicorn's caller info points to uvicorn internals, which is not useful. The <code>logger</code> field (e.g., <code>uvicorn.error</code>, <code>uvicorn.access</code>) already identifies the source.</p>"},{"location":"reference/logging/#grelmicro.logging.uvicorn.UvicornAccessFormatter","title":"UvicornAccessFormatter","text":"<pre><code>UvicornAccessFormatter()\n</code></pre> <p>               Bases: <code>UvicornFormatter</code></p> <p>Format-aware uvicorn access log formatter.</p> <p>Parses uvicorn's access log tuple arguments into structured fields (<code>client_addr</code>, <code>method</code>, <code>full_path</code>, <code>http_version</code>, <code>status_code</code>).</p>"},{"location":"reference/logging/#grelmicro.logging.uvicorn.UvicornAccessFormatter.format","title":"format","text":"<pre><code>format(record: LogRecord) -&gt; str\n</code></pre> <p>Format access records with split request fields.</p>"},{"location":"reference/resilience/","title":"Resilience","text":"<p>Resilience.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker","title":"CircuitBreaker","text":"<pre><code>CircuitBreaker(\n    name: Annotated[\n        str,\n        Doc(\n            \"\\n                Name of the circuit breaker instance.\\n                \"\n        ),\n    ],\n    *,\n    ignore_exceptions: Annotated[\n        type[Exception] | tuple[type[Exception], ...],\n        Doc(\n            \"\\n                Exceptions that are ignored and do not count as errors.\\n                \"\n        ),\n    ] = (),\n    error_threshold: Annotated[\n        int,\n        Doc(\n            \"\\n                Number of consecutive errors before opening the circuit.\\n                \"\n        ),\n    ] = 5,\n    success_threshold: Annotated[\n        int,\n        Doc(\n            \"\\n                Number of consecutive successes in HALF_OPEN state before closing the circuit.\\n                \"\n        ),\n    ] = 2,\n    reset_timeout: Annotated[\n        float,\n        Doc(\n            \"\\n                Seconds the circuit stays OPEN before transitioning to HALF_OPEN.\\n                \"\n        ),\n    ] = 30,\n    half_open_capacity: Annotated[\n        int,\n        Doc(\n            \"\\n                Maximum number of concurrent calls allowed in the HALF_OPEN state.\\n                \"\n        ),\n    ] = 1,\n    log_level: Annotated[\n        Literal[\n            \"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"\n        ],\n        Doc(\n            \"\\n                Logging level for the circuit breaker. Defaults to INFO.\\n                \"\n        ),\n    ] = \"WARNING\",\n)\n</code></pre> <p>Circuit Breaker.</p> <p>Implements the circuit breaker pattern to prevent cascading errors by monitoring and controlling calls to a protected service.</p> <p>Initialize the circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.from_thread","title":"from_thread  <code>property</code>","text":"<pre><code>from_thread: _ThreadAdapter\n</code></pre> <p>Return the lock adapter for worker thread.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.name","title":"name  <code>property</code>","text":"<pre><code>name: str\n</code></pre> <p>Return the name of the circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.state","title":"state  <code>property</code>","text":"<pre><code>state: CircuitBreakerState\n</code></pre> <p>Return the current state of the circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.last_error","title":"last_error  <code>property</code>","text":"<pre><code>last_error: Exception | None\n</code></pre> <p>Return the last error recorded by the circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.last_error_time","title":"last_error_time  <code>property</code>","text":"<pre><code>last_error_time: datetime | None\n</code></pre> <p>Return the time of the last error recorded by the circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.log_level","title":"log_level  <code>property</code> <code>writable</code>","text":"<pre><code>log_level: str\n</code></pre> <p>Return the logging level of the circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.__call__","title":"__call__","text":"<pre><code>__call__(\n    func: Callable[..., Any] | None = None,\n) -&gt; Callable[..., Any]\n</code></pre> <p>Return a decorator that protects a function with the circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Circuit breaker context manager.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; bool | None\n</code></pre> <p>Exit the context manager.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.metrics","title":"metrics","text":"<pre><code>metrics() -&gt; CircuitBreakerMetrics\n</code></pre> <p>Return current metrics for this circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.restart","title":"restart  <code>async</code>","text":"<pre><code>restart() -&gt; None\n</code></pre> <p>Restart the circuit breaker, clearing all counts and resetting to CLOSED state.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.transition_to_closed","title":"transition_to_closed  <code>async</code>","text":"<pre><code>transition_to_closed() -&gt; None\n</code></pre> <p>Transition the circuit breaker to CLOSED state.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.transition_to_open","title":"transition_to_open  <code>async</code>","text":"<pre><code>transition_to_open(until: float | None = None) -&gt; None\n</code></pre> <p>Transition the circuit breaker to OPEN state.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.transition_to_half_open","title":"transition_to_half_open  <code>async</code>","text":"<pre><code>transition_to_half_open() -&gt; None\n</code></pre> <p>Transition the circuit breaker to HALF_OPEN state.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.transition_to_forced_open","title":"transition_to_forced_open  <code>async</code>","text":"<pre><code>transition_to_forced_open() -&gt; None\n</code></pre> <p>Transition the circuit breaker to FORCED_OPEN state.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreaker.transition_to_forced_closed","title":"transition_to_forced_closed  <code>async</code>","text":"<pre><code>transition_to_forced_closed() -&gt; None\n</code></pre> <p>Transition the circuit breaker to FORCED_CLOSED state.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreakerError","title":"CircuitBreakerError","text":"<pre><code>CircuitBreakerError(\n    *,\n    name: str,\n    last_error_time: datetime | None = None,\n    last_error: Exception | None = None,\n)\n</code></pre> <p>               Bases: <code>ResilienceError</code></p> <p>Circuit breaker error.</p> <p>Raised when calls are not permitted by the circuit breaker.</p> <p>Initialize the error.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreakerMetrics","title":"CircuitBreakerMetrics","text":"<p>               Bases: <code>BaseModel</code></p> <p>Circuit breaker metrics.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreakerState","title":"CircuitBreakerState","text":"<p>               Bases: <code>StrEnum</code></p> <p>Circuit breaker state.</p> <p>State machine diagram: <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 errors &gt;= threshold  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 CLOSED \u2502\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500&gt; \u2502  OPEN  \u2502 &lt;\u2500\u2510\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                      \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518   \u2502\n    \u25b2                       timeout \u2502        \u2502 errors &gt;= threshold\n    \u2502                               \u25bc        \u2502\n    \u2502                         \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u2502\n    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2502 HALF_OPEN \u2502\u2500\u2500\u2518\n      success &gt;= threshold    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre></p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreakerState.CLOSED","title":"CLOSED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>CLOSED = 'CLOSED'\n</code></pre> <p>Circuit is closed, calls are allowed.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreakerState.OPEN","title":"OPEN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>OPEN = 'OPEN'\n</code></pre> <p>Circuit is open, calls are not allowed.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreakerState.HALF_OPEN","title":"HALF_OPEN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>HALF_OPEN = 'HALF_OPEN'\n</code></pre> <p>Circuit is half-open, calls are limited.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreakerState.FORCED_OPEN","title":"FORCED_OPEN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FORCED_OPEN = 'FORCED_OPEN'\n</code></pre> <p>Circuit is open for an indefinite time, calls are not allowed.</p>"},{"location":"reference/resilience/#grelmicro.resilience.CircuitBreakerState.FORCED_CLOSED","title":"FORCED_CLOSED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FORCED_CLOSED = 'FORCED_CLOSED'\n</code></pre> <p>Circuit is forced closed for an indefinite time, calls are allowed.</p>"},{"location":"reference/resilience/#grelmicro.resilience.ErrorDetails","title":"ErrorDetails","text":"<p>               Bases: <code>BaseModel</code></p> <p>Details about an error recorded by the circuit breaker.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiter","title":"RateLimiter","text":"<pre><code>RateLimiter(\n    name: Annotated[\n        str, Doc(\"Name of the rate limiter instance.\")\n    ],\n    *,\n    limit: Annotated[\n        PositiveInt,\n        Doc(\"Maximum number of requests per window.\"),\n    ],\n    window: Annotated[\n        PositiveFloat, Doc(\"Window duration in seconds.\")\n    ],\n)\n</code></pre> <p>Rate limiter using Generic Cell Rate Algorithm (GCRA).</p> <p>Implements sliding-window rate limiting by tracking the theoretical arrival time of requests. Limits the number of calls within a time window per key. Requires a rate limiter backend (Redis or Memory).</p> <p>Initialize the rate limiter.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiter.config","title":"config  <code>property</code>","text":"<pre><code>config: RateLimiterConfig\n</code></pre> <p>Return the config.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiter.acquire","title":"acquire  <code>async</code>","text":"<pre><code>acquire(\n    *,\n    key: Annotated[\n        str,\n        Doc(\n            \"Identifier for rate limiting (e.g. IP address, user ID, session).\"\n        ),\n    ],\n    cost: Annotated[\n        int, Doc(\"Number of tokens to consume.\")\n    ] = 1,\n) -&gt; RateLimitResult\n</code></pre> <p>Check rate limit and consume tokens if allowed.</p> <p>Returns:</p> Type Description <code>RateLimitResult</code> <p>RateLimitResult with allowed, limit, remaining,</p> <code>RateLimitResult</code> <p>retry_after, and reset_after fields.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If cost is less than 1 or greater than limit.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiter.acquire_or_raise","title":"acquire_or_raise  <code>async</code>","text":"<pre><code>acquire_or_raise(\n    *,\n    key: Annotated[\n        str,\n        Doc(\n            \"Identifier for rate limiting (e.g. IP address, user ID, session).\"\n        ),\n    ],\n    cost: Annotated[\n        int, Doc(\"Number of tokens to consume.\")\n    ] = 1,\n) -&gt; RateLimitResult\n</code></pre> <p>Check rate limit; raise if exceeded.</p> <p>Returns:</p> Type Description <code>RateLimitResult</code> <p>RateLimitResult if allowed.</p> <p>Raises:</p> Type Description <code>RateLimitExceededError</code> <p>If the rate limit is exceeded.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiterConfig","title":"RateLimiterConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Rate Limiter Config.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimitExceededError","title":"RateLimitExceededError","text":"<pre><code>RateLimitExceededError(*, key: str, retry_after: float)\n</code></pre> <p>               Bases: <code>ResilienceError</code></p> <p>Rate limit exceeded error.</p> <p>Raised when a rate limit check fails (too many requests).</p> <p>Initialize the error.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimitResult","title":"RateLimitResult","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Result of a rate limit check.</p> <p>Fields map to HTTP rate limit headers: - <code>allowed</code> -&gt; 200 vs 429 status - <code>limit</code> -&gt; <code>X-RateLimit-Limit</code> / <code>RateLimit-Policy: ;q=</code> - <code>remaining</code> -&gt; <code>X-RateLimit-Remaining</code> / <code>RateLimit: ;r=</code> - <code>retry_after</code> -&gt; <code>Retry-After</code> header - <code>reset_after</code> -&gt; <code>X-RateLimit-Reset</code> / <code>RateLimit: ;t=</code></p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimitResult.allowed","title":"allowed  <code>instance-attribute</code>","text":"<pre><code>allowed: bool\n</code></pre> <p>Whether the request is permitted.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimitResult.limit","title":"limit  <code>instance-attribute</code>","text":"<pre><code>limit: int\n</code></pre> <p>Total quota for the window.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimitResult.remaining","title":"remaining  <code>instance-attribute</code>","text":"<pre><code>remaining: int\n</code></pre> <p>Remaining requests in the current window.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimitResult.retry_after","title":"retry_after  <code>instance-attribute</code>","text":"<pre><code>retry_after: float\n</code></pre> <p>Seconds until the next request is allowed (0.0 if allowed).</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimitResult.reset_after","title":"reset_after  <code>instance-attribute</code>","text":"<pre><code>reset_after: float\n</code></pre> <p>Seconds until the full quota resets.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiterBackend","title":"RateLimiterBackend","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for rate limiter storage backends.</p> <p>Implementations must be async context managers and provide a single <code>acquire</code> method for atomic rate limit checks.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiterBackend.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Open the backend connection.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiterBackend.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; None\n</code></pre> <p>Close the backend connection.</p>"},{"location":"reference/resilience/#grelmicro.resilience.RateLimiterBackend.acquire","title":"acquire  <code>async</code>","text":"<pre><code>acquire(\n    *, key: str, limit: int, window: float, cost: int\n) -&gt; RateLimitResult\n</code></pre> <p>Try to acquire rate limit tokens.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The rate limit key (e.g. IP, user ID, session).</p> required <code>limit</code> <code>int</code> <p>Maximum number of requests allowed in the window.</p> required <code>window</code> <code>float</code> <p>Window duration in seconds.</p> required <code>cost</code> <code>int</code> <p>Number of tokens to consume.</p> required <p>Returns:</p> Type Description <code>RateLimitResult</code> <p>RateLimitResult with allowed, limit, remaining,</p> <code>RateLimitResult</code> <p>retry_after, and reset_after fields.</p>"},{"location":"reference/resilience/#grelmicro.resilience.ResilienceSettingsValidationError","title":"ResilienceSettingsValidationError","text":"<pre><code>ResilienceSettingsValidationError(\n    error: ValidationError | str,\n)\n</code></pre> <p>               Bases: <code>ResilienceError</code>, <code>SettingsValidationError</code></p> <p>Resilience Settings Validation Error.</p>"},{"location":"reference/sync/","title":"Synchronization","text":"<p>Synchronization.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection","title":"LeaderElection","text":"<pre><code>LeaderElection(\n    name: Annotated[\n        str,\n        Doc(\n            \"\\n                The name of the resource representing the leader election.\\n\\n                It will be used as the lock name so make sure it is unique on the distributed lock\\n                backend.\\n                \"\n        ),\n    ],\n    *,\n    backend: Annotated[\n        SyncBackend | None,\n        Doc(\n            \"\\n                The distributed lock backend used to acquire and release the lock.\\n\\n                By default, it will use the lock backend registry to get the default lock backend.\\n                \"\n        ),\n    ] = None,\n    worker: Annotated[\n        str | UUID | None,\n        Doc(\n            \"\\n                The worker identity.\\n\\n                By default, a UUIDv1 will be generated.\\n                \"\n        ),\n    ] = None,\n    lease_duration: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The duration in seconds after the lock will be released if not renewed.\\n\\n                If the worker becomes unavailable, the lock can only be acquired by an other worker\\n                after it has expired.\\n                \"\n        ),\n    ] = 15,\n    renew_deadline: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The duration in seconds that the leader worker will try to acquire the lock before\\n                giving up.\\n\\n                Must be shorter than the lease duration. In case of multiple errors, the leader\\n                worker will lose the lead to prevent split-brain scenarios and ensure that only one\\n                worker is the leader at any time.\\n                \"\n        ),\n    ] = 10,\n    retry_interval: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The duration in seconds between attempts to acquire or renew the lock.\\n\\n                Must be shorter than the renew deadline. A shorter schedule enables faster leader\\n                elections but may increase load on the distributed lock backend, while a longer\\n                schedule reduces load but can delay new leader elections.\\n                \"\n        ),\n    ] = 2,\n    backend_timeout: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The duration in seconds for waiting on backend for acquiring and releasing the lock.\\n\\n                This value determines how long the system will wait before giving up the current\\n                operation.\\n                \"\n        ),\n    ] = 5,\n    error_interval: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The duration in seconds between logging error messages.\\n\\n                If shorter than the retry interval, it will log every error. It is used to prevent\\n                flooding the logs when the lock backend is unavailable.\\n                \"\n        ),\n    ] = 30,\n)\n</code></pre> <p>               Bases: <code>SyncPrimitive</code>, <code>Task</code></p> <p>Leader Election.</p> <p>The leader election is a synchronization primitive with the worker as scope. It runs as a task to acquire or renew the distributed lock.</p> <p>Initialize the leader election.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.name","title":"name  <code>property</code>","text":"<pre><code>name: str\n</code></pre> <p>Return the task name.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Wait for the leader with the context manager.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; bool | None\n</code></pre> <p>Exit the context manager (no-op).</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.is_running","title":"is_running","text":"<pre><code>is_running() -&gt; bool\n</code></pre> <p>Check if the leader election task is running.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.is_leader","title":"is_leader","text":"<pre><code>is_leader() -&gt; bool\n</code></pre> <p>Check if the current worker is the leader.</p> <p>To avoid a split-brain scenario, the leader considers itself as no longer leader if the renew deadline is reached.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the current worker is the leader, False otherwise.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.wait_for_leader","title":"wait_for_leader  <code>async</code>","text":"<pre><code>wait_for_leader() -&gt; None\n</code></pre> <p>Wait until the current worker is the leader.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.wait_lose_leader","title":"wait_lose_leader  <code>async</code>","text":"<pre><code>wait_lose_leader() -&gt; None\n</code></pre> <p>Wait until the current worker is no longer the leader.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.__call__","title":"__call__  <code>async</code>","text":"<pre><code>__call__(\n    *, task_status: TaskStatus[None] = TASK_STATUS_IGNORED\n) -&gt; None\n</code></pre> <p>Run polling loop service to acquire or renew the distributed lock.</p>"},{"location":"reference/sync/#grelmicro.sync.LeaderElection.guard","title":"guard","text":"<pre><code>guard() -&gt; _LeaderGuard\n</code></pre> <p>Return a non-blocking synchronization guard.</p> <p>The guard raises <code>WouldBlock</code> if the current worker is not the leader, making it suitable for use as the <code>sync</code> parameter of <code>IntervalTask</code>.</p> <p>Unlike using <code>LeaderElection</code> directly (which blocks until leader), the guard skips the current tick and retries on the next interval.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock","title":"Lock","text":"<pre><code>Lock(\n    name: Annotated[\n        str,\n        Doc(\n            \"\\n                The name of the resource to lock.\\n\\n                It will be used as the lock name so make sure it is unique on the lock backend.\\n                \"\n        ),\n    ],\n    *,\n    backend: Annotated[\n        SyncBackend | None,\n        Doc(\n            \"\\n                The distributed lock backend used to acquire and release the lock.\\n\\n                By default, it will use the lock backend registry to get the default lock backend.\\n                \"\n        ),\n    ] = None,\n    worker: Annotated[\n        str | UUID | None,\n        Doc(\n            \"\\n                The worker identity.\\n\\n                By default, a UUIDv1 will be generated.\\n                \"\n        ),\n    ] = None,\n    lease_duration: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The duration in seconds for the lock to be held by default.\\n                \"\n        ),\n    ] = 60,\n    retry_interval: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The duration in seconds between attempts to acquire the lock.\\n\\n                Should be greater or equal than 0.1 to prevent flooding the lock backend.\\n                \"\n        ),\n    ] = 0.1,\n)\n</code></pre> <p>               Bases: <code>BaseLock</code></p> <p>Lock.</p> <p>This lock is a distributed lock that is used to acquire a resource across multiple workers. The lock is acquired asynchronously and can be extended multiple times manually. The lock is automatically released after a duration if not extended.</p> <p>Initialize the lock.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.config","title":"config  <code>property</code>","text":"<pre><code>config: LockConfig\n</code></pre> <p>Return the lock config.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.from_thread","title":"from_thread  <code>property</code>","text":"<pre><code>from_thread: ThreadLockAdapter\n</code></pre> <p>Return the lock adapter for worker thread.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Acquire the lock with the async context manager.</p> <p>Raises:</p> Type Description <code>LockReentrantError</code> <p>If the lock is already acquired (nested usage is not supported).</p> <code>LockAcquireError</code> <p>If the lock cannot be acquired due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; bool | None\n</code></pre> <p>Release the lock with the async context manager.</p> <p>Raises:</p> Type Description <code>LockNotOwnedError</code> <p>If the lock is not owned by the current token.</p> <code>LockReleaseError</code> <p>If the lock cannot be released due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.acquire","title":"acquire  <code>async</code>","text":"<pre><code>acquire() -&gt; None\n</code></pre> <p>Acquire the lock.</p> <p>Raises:</p> Type Description <code>LockReentrantError</code> <p>If the lock is already acquired (nested usage is not supported).</p> <code>LockAcquireError</code> <p>If the lock cannot be acquired due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.acquire_nowait","title":"acquire_nowait  <code>async</code>","text":"<pre><code>acquire_nowait() -&gt; None\n</code></pre> <p>Acquire the lock, without blocking.</p> <p>Raises:</p> Type Description <code>LockReentrantError</code> <p>If the lock is already acquired (nested usage is not supported).</p> <code>WouldBlock</code> <p>If the lock cannot be acquired without blocking.</p> <code>LockAcquireError</code> <p>If the lock cannot be acquired due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.release","title":"release  <code>async</code>","text":"<pre><code>release() -&gt; None\n</code></pre> <p>Release the lock.</p> <p>Raises:</p> Type Description <code>LockNotOwnedError</code> <p>If the lock is not owned by the current token.</p> <code>LockReleaseError</code> <p>If the lock cannot be released due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.locked","title":"locked  <code>async</code>","text":"<pre><code>locked() -&gt; bool\n</code></pre> <p>Check if the lock is acquired.</p> <p>Raises:</p> Type Description <code>LockLockedCheckError</code> <p>If the lock cannot be checked due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.owned","title":"owned  <code>async</code>","text":"<pre><code>owned() -&gt; bool\n</code></pre> <p>Check if the lock is owned by the current token.</p> <p>Raises:</p> Type Description <code>SyncBackendError</code> <p>If the lock cannot be checked due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.do_acquire","title":"do_acquire  <code>async</code>","text":"<pre><code>do_acquire(token: str) -&gt; bool\n</code></pre> <p>Acquire the lock.</p> <p>This method should not be called directly. Use <code>acquire</code> instead.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the lock was acquired, False if the lock was not acquired.</p> <p>Raises:</p> Type Description <code>LockAcquireError</code> <p>If the lock cannot be acquired due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.do_release","title":"do_release  <code>async</code>","text":"<pre><code>do_release(token: str) -&gt; bool\n</code></pre> <p>Release the lock.</p> <p>This method should not be called directly. Use <code>release</code> instead.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the lock was released, False otherwise.</p> <p>Raises:</p> Type Description <code>LockReleaseError</code> <p>Cannot release the lock due to backend error.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.do_owned","title":"do_owned  <code>async</code>","text":"<pre><code>do_owned(token: str) -&gt; bool\n</code></pre> <p>Check if the lock is owned by the current token.</p> <p>This method should not be called directly. Use <code>owned</code> instead.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the lock is owned by the current token, False otherwise.</p> <p>Raises:</p> Type Description <code>LockOwnedCheckError</code> <p>Cannot check if the lock is owned due to backend error.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.do_thread_acquire","title":"do_thread_acquire  <code>async</code>","text":"<pre><code>do_thread_acquire(thread_id: int) -&gt; None\n</code></pre> <p>Acquire the lock from a worker thread (blocking).</p> <p>Runs on the event loop so the reentrant check and backend acquire are atomic with respect to other threads.</p> <p>Raises:</p> Type Description <code>LockReentrantError</code> <p>If the lock is already acquired (nested usage is not supported).</p> <code>LockAcquireError</code> <p>If the lock cannot be acquired due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.do_thread_acquire_nowait","title":"do_thread_acquire_nowait  <code>async</code>","text":"<pre><code>do_thread_acquire_nowait(thread_id: int) -&gt; None\n</code></pre> <p>Acquire the lock from a worker thread (non-blocking).</p> <p>Runs on the event loop so the reentrant check and backend acquire are atomic with respect to other threads.</p> <p>Raises:</p> Type Description <code>LockReentrantError</code> <p>If the lock is already acquired (nested usage is not supported).</p> <code>WouldBlock</code> <p>If the lock cannot be acquired without blocking.</p> <code>LockAcquireError</code> <p>If the lock cannot be acquired due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.Lock.do_thread_release","title":"do_thread_release  <code>async</code>","text":"<pre><code>do_thread_release(thread_id: int) -&gt; None\n</code></pre> <p>Release the lock from a worker thread.</p> <p>Runs on the event loop so the backend release is atomic with respect to other threads.</p> <p>Raises:</p> Type Description <code>LockNotOwnedError</code> <p>If the lock is not owned by the current token.</p> <code>LockReleaseError</code> <p>If the lock cannot be released due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock","title":"TaskLock","text":"<pre><code>TaskLock(\n    name: Annotated[\n        str,\n        Doc(\n            \"\\n                The name of the resource to lock.\\n\\n                It will be used as the lock name so make sure it is unique on the lock backend.\\n                \"\n        ),\n    ],\n    *,\n    backend: Annotated[\n        SyncBackend | None,\n        Doc(\n            \"\\n                The distributed lock backend used to acquire and release the lock.\\n\\n                By default, it will use the lock backend registry to get the default lock backend.\\n                \"\n        ),\n    ] = None,\n    worker: Annotated[\n        str | UUID | None,\n        Doc(\n            \"\\n                The worker identity.\\n\\n                By default, a UUIDv1 will be generated.\\n                \"\n        ),\n    ] = None,\n    min_lock_seconds: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The minimum duration in seconds to hold the lock after task completion.\\n\\n                Prevents re-execution on other nodes before this duration has elapsed.\\n                \"\n        ),\n    ] = 1,\n    max_lock_seconds: Annotated[\n        Seconds,\n        Doc(\n            \"\\n                The maximum duration in seconds to hold the lock (deadlock protection).\\n\\n                Acts as the TTL on acquire.\\n                \"\n        ),\n    ] = 60,\n)\n</code></pre> <p>               Bases: <code>SyncPrimitive</code></p> <p>Task Lock.</p> <p>A distributed lock for scheduled tasks. Unlike a regular Lock, TaskLock does not release immediately on context manager exit. Instead, it keeps the lock held for at least <code>min_lock_seconds</code> seconds to prevent re-execution on other nodes.</p> <p>There is no background task that maintains the lock active during execution. The lock relies entirely on the TTL (<code>max_lock_seconds</code>) set at acquire time.</p> <p>This lock is designed to be used as the <code>sync</code> parameter of <code>IntervalTask</code>.</p> <p>Initialize the task lock.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.config","title":"config  <code>property</code>","text":"<pre><code>config: TaskLockConfig\n</code></pre> <p>Return the task lock config.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.from_thread","title":"from_thread  <code>property</code>","text":"<pre><code>from_thread: ThreadTaskLockAdapter\n</code></pre> <p>Return the task lock adapter for worker thread.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Acquire the lock with duration=max_lock_seconds.</p> <p>Raises:</p> Type Description <code>WouldBlock</code> <p>If the lock is already held by another worker.</p> <code>LockAcquireError</code> <p>If the lock cannot be acquired due to a backend error.</p> <code>LockReentrantError</code> <p>If the lock is already acquired (nested usage is not supported).</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; bool | None\n</code></pre> <p>Release or extend the lock based on elapsed time.</p> <p>If elapsed &gt;= min_lock_seconds, release immediately. If elapsed &lt; min_lock_seconds, re-acquire with remaining duration (let TTL expire).</p> <p>Raises:</p> Type Description <code>LockReleaseError</code> <p>If the lock cannot be released due to a backend error.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.locked","title":"locked  <code>async</code>","text":"<pre><code>locked() -&gt; bool\n</code></pre> <p>Check if the lock is acquired.</p> <p>Raises:</p> Type Description <code>LockLockedCheckError</code> <p>If the lock cannot be checked due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.do_acquire","title":"do_acquire  <code>async</code>","text":"<pre><code>do_acquire(token: str) -&gt; bool\n</code></pre> <p>Acquire the lock.</p> <p>This method should not be called directly. Use the context manager instead.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the lock was acquired, False if the lock was not acquired.</p> <p>Raises:</p> Type Description <code>LockAcquireError</code> <p>If the lock cannot be acquired due to an error on the backend.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.do_release","title":"do_release  <code>async</code>","text":"<pre><code>do_release(token: str) -&gt; bool\n</code></pre> <p>Release the lock.</p> <p>This method should not be called directly. Use the context manager instead.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the lock was released, False otherwise.</p> <p>Raises:</p> Type Description <code>LockReleaseError</code> <p>Cannot release the lock due to backend error.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.do_reacquire","title":"do_reacquire  <code>async</code>","text":"<pre><code>do_reacquire(token: str, duration: float) -&gt; bool\n</code></pre> <p>Re-acquire the lock with a specific duration.</p> <p>This method should not be called directly. Use the context manager instead.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the lock was re-acquired, False otherwise.</p> <p>Raises:</p> Type Description <code>LockReleaseError</code> <p>Cannot re-acquire the lock due to backend error.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.do_thread_enter","title":"do_thread_enter  <code>async</code>","text":"<pre><code>do_thread_enter() -&gt; None\n</code></pre> <p>Acquire the lock from a worker thread.</p> <p>Runs entirely on the event loop so the reentrant check, token generation, and backend acquire are atomic with respect to other threads.</p> <p>Raises:</p> Type Description <code>WouldBlock</code> <p>If the lock is already held by another worker.</p> <code>LockAcquireError</code> <p>If the lock cannot be acquired due to a backend error.</p> <code>LockReentrantError</code> <p>If the lock is already acquired (nested usage is not supported).</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.do_thread_exit","title":"do_thread_exit  <code>async</code>","text":"<pre><code>do_thread_exit() -&gt; None\n</code></pre> <p>Release or extend the lock from a worker thread.</p> <p>Runs entirely on the event loop so the token generation and backend release are atomic with respect to other threads.</p> <p>Raises:</p> Type Description <code>LockReleaseError</code> <p>If the lock cannot be released due to a backend error.</p>"},{"location":"reference/sync/#grelmicro.sync.TaskLock.do_exit","title":"do_exit  <code>async</code>","text":"<pre><code>do_exit(token: str) -&gt; None\n</code></pre> <p>Handle exit logic: release or re-acquire based on elapsed time.</p>"},{"location":"reference/task/","title":"Task","text":"<p>Task.</p>"},{"location":"reference/task/#grelmicro.task.TaskManager","title":"TaskManager","text":"<pre><code>TaskManager(\n    *,\n    auto_start: Annotated[\n        bool,\n        Doc(\n            \"\\n                Automatically start all tasks.\\n                \"\n        ),\n    ] = True,\n    tasks: Annotated[\n        list[Task] | None,\n        Doc(\n            \"\\n                A list of tasks to be started.\\n                \"\n        ),\n    ] = None,\n)\n</code></pre> <p>               Bases: <code>TaskRouter</code></p> <p>Task Manager.</p> <p><code>TaskManager</code> class, the main entrypoint to manage scheduled tasks.</p> <p>Initialize the task manager.</p>"},{"location":"reference/task/#grelmicro.task.TaskManager.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Enter the context manager.</p>"},{"location":"reference/task/#grelmicro.task.TaskManager.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc_value: BaseException | None,\n    traceback: TracebackType | None,\n) -&gt; bool | None\n</code></pre> <p>Exit the context manager.</p>"},{"location":"reference/task/#grelmicro.task.TaskManager.start","title":"start  <code>async</code>","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start all tasks manually.</p>"},{"location":"reference/task/#grelmicro.task.TaskRouter","title":"TaskRouter","text":"<pre><code>TaskRouter(\n    *,\n    tasks: Annotated[\n        list[Task] | None,\n        Doc(\n            \"\\n                A list of tasks to be scheduled.\\n                \"\n        ),\n    ] = None,\n)\n</code></pre> <p>Task Router.</p> <p><code>TaskRouter</code> class, used to group task schedules, for example to structure an app in multiple files. It would then be included in the <code>TaskManager</code>, or in another <code>TaskRouter</code>.</p> <p>Initialize the task router.</p>"},{"location":"reference/task/#grelmicro.task.TaskRouter.tasks","title":"tasks  <code>property</code>","text":"<pre><code>tasks: list[Task]\n</code></pre> <p>List of scheduled tasks.</p>"},{"location":"reference/task/#grelmicro.task.TaskRouter.add_task","title":"add_task","text":"<pre><code>add_task(task: Task) -&gt; None\n</code></pre> <p>Add a task to the scheduler.</p>"},{"location":"reference/task/#grelmicro.task.TaskRouter.interval","title":"interval","text":"<pre><code>interval(\n    *,\n    seconds: Annotated[\n        float,\n        Doc(\n            \"\\n                The duration in seconds between each task run.\\n\\n                Accuracy is not guaranteed and may vary with system load. Consider the\\n                execution time of the task when setting the interval.\\n                \"\n        ),\n    ],\n    name: Annotated[\n        str | None,\n        Doc(\n            \"\\n                The name of the task.\\n\\n                If None, a name will be generated automatically from the function.\\n                Also used as the lock name when distributed locking is enabled.\\n                \"\n        ),\n    ] = None,\n    max_lock_seconds: Annotated[\n        float | None,\n        Doc(\n            \"\\n                The maximum duration in seconds to hold the lock (crash protection).\\n\\n                Setting this enables distributed locking: the task runs at most once\\n                per interval across all workers. Must be &gt;= ``seconds``.\\n                When ``leader`` is set without this, defaults to ``seconds * 5``.\\n                \"\n        ),\n    ] = None,\n    min_lock_seconds: Annotated[\n        float | None,\n        Doc(\n            \"\\n                The minimum duration in seconds to hold the lock after task completion.\\n\\n                Prevents re-execution on other nodes before this duration has elapsed.\\n                Defaults to ``seconds`` when distributed locking is enabled.\\n                Requires ``max_lock_seconds`` or ``leader`` to be set.\\n                \"\n        ),\n    ] = None,\n    leader: Annotated[\n        LeaderElection | None,\n        Doc(\n            \"\\n                Optional leader election for leader gating.\\n\\n                When provided, the task only executes on the leader worker.\\n                Implies distributed locking (lock is automatically configured).\\n                \"\n        ),\n    ] = None,\n    backend: Annotated[\n        SyncBackend | None,\n        Doc(\n            \"\\n                The distributed lock backend.\\n\\n                By default, uses the lock backend registry.\\n                Only used when distributed locking is enabled.\\n                \"\n        ),\n    ] = None,\n    worker: Annotated[\n        str | UUID | None,\n        Doc(\n            \"\\n                The worker identity.\\n\\n                By default, a UUIDv1 will be generated.\\n                Only used when distributed locking is enabled.\\n                \"\n        ),\n    ] = None,\n    sync: Annotated[\n        SyncPrimitive | None,\n        Doc(\n            \"\\n                The synchronization primitive to use for the task.\\n\\n                Use a ``Lock`` to synchronize access to a shared resource.\\n\\n                .. deprecated:: 0.6.0\\n                    Using ``sync`` with ``TaskLock`` or ``LeaderElection`` is deprecated\\n                    and will be removed in 0.7.0.\\n                    Use ``max_lock_seconds`` and ``leader`` parameters instead.\\n\\n                If None, no synchronization is used and the task will run on all workers.\\n                \"\n        ),\n    ] = None,\n) -&gt; Callable[\n    [Callable[..., Any | Awaitable[Any]]],\n    Callable[..., Any | Awaitable[Any]],\n]\n</code></pre> <p>Decorate function to add it as an interval task.</p> <p>Supports three modes:</p> <ul> <li>Local: No lock params, runs on every worker, every interval.</li> <li>Distributed lock: Set <code>max_lock_seconds</code> to run at most once per   interval across all workers.</li> <li>Leader-gated: Set <code>leader</code> to restrict execution to the leader   worker (lock is implied).</li> </ul> <p>Raises:</p> Type Description <code>FunctionTypeError</code> <p>If the task name generation fails.</p> <code>ValueError</code> <p>If seconds is less than or equal to 0.</p> <code>ValueError</code> <p>If deprecated sync (non-Lock) is combined with max_lock_seconds, min_lock_seconds, or leader.</p> <code>ValueError</code> <p>If max_lock_seconds is less than seconds.</p> <code>ValueError</code> <p>If min_lock_seconds is set without max_lock_seconds or leader.</p> <code>ValueError</code> <p>If min_lock_seconds is greater than max_lock_seconds.</p>"},{"location":"reference/task/#grelmicro.task.TaskRouter.scheduled","title":"scheduled","text":"<pre><code>scheduled(\n    *,\n    seconds: Annotated[\n        float,\n        Doc(\n            \"\\n                The duration in seconds between each scheduling attempt.\\n\\n                Each worker retries every N seconds, but only one worker executes\\n                per interval thanks to the built-in lock. Also used as the\\n                ``min_lock_seconds`` value.\\n                \"\n        ),\n    ],\n    name: Annotated[\n        str | None,\n        Doc(\n            \"\\n                The name of the task.\\n\\n                If None, a name will be generated automatically from the function.\\n                Also used as the TaskLock name.\\n                \"\n        ),\n    ] = None,\n    max_lock_seconds: Annotated[\n        float | None,\n        Doc(\n            \"\\n                The maximum duration in seconds to hold the lock (crash protection).\\n\\n                Defaults to ``seconds * 5``. Must be &gt;= ``seconds``.\\n                \"\n        ),\n    ] = None,\n    leader: Annotated[\n        LeaderElection | None,\n        Doc(\n            \"\\n                Optional leader election for leader gating.\\n\\n                When provided, the task only executes on the leader worker.\\n                \"\n        ),\n    ] = None,\n    backend: Annotated[\n        SyncBackend | None,\n        Doc(\n            \"\\n                The distributed lock backend.\\n\\n                By default, uses the lock backend registry.\\n                \"\n        ),\n    ] = None,\n    worker: Annotated[\n        str | UUID | None,\n        Doc(\n            \"\\n                The worker identity.\\n\\n                By default, a UUIDv1 will be generated.\\n                \"\n        ),\n    ] = None,\n) -&gt; Callable[\n    [Callable[..., Any | Awaitable[Any]]],\n    Callable[..., Any | Awaitable[Any]],\n]\n</code></pre> <p>Decorate function to add it as a distributed scheduled task.</p> <p>.. deprecated:: 0.6.0     Use <code>interval()</code> with <code>max_lock_seconds</code> instead.     Will be removed in 0.7.0.</p> <p>The task runs at most once per interval across all workers, using a built-in TaskLock. Can optionally be gated behind a leader election.</p> <p>Raises:</p> Type Description <code>FunctionTypeError</code> <p>If the task name generation fails.</p> <code>ValueError</code> <p>If seconds is less than or equal to 0.</p> <code>ValueError</code> <p>If max_lock_seconds is less than seconds.</p>"},{"location":"reference/task/#grelmicro.task.TaskRouter.include_router","title":"include_router","text":"<pre><code>include_router(router: TaskRouter) -&gt; None\n</code></pre> <p>Include another router in this router.</p>"},{"location":"reference/task/#grelmicro.task.TaskRouter.started","title":"started","text":"<pre><code>started() -&gt; bool\n</code></pre> <p>Check if the task manager has started.</p>"},{"location":"reference/task/#grelmicro.task.TaskRouter.do_mark_as_started","title":"do_mark_as_started","text":"<pre><code>do_mark_as_started() -&gt; None\n</code></pre> <p>Mark the task manager as started.</p> <p>Do not call this method directly. It is called by the task manager when the task manager is started.</p>"}]}