codecov:
  require_ci_to_pass: true
coverage:
  status:
    project:
      default:
        target: auto # do not regress from base branch
        if_ci_failed: error
      unit:
        target: 90% # unit test coverage
        flags:
          - unit
        if_ci_failed: error
